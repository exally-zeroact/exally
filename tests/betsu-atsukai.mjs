/* ★実Excel と 同じに ならない と 決めた 物★（1つずつ 訳を 書く）
 *
 *  ★なぜ 別の 紙に 出したか★（2026-09-14）
 *    ・前は `tests/formula-yosoku.test.mjs` の 中に 書いて 在りました。
 *    ・そうすると ★突き合わせの 道具が 同じ 名簿を もう一度 手で 持つ★事に なる。
 *    ・⇒ 物差し（`tests/monosashi.mjs`）・材料（紙の `#材料`）と 同じで ★1か所★に する。
 *
 *  ★ここに 書かずに 黙って 外すと『合っている』の 意味が 薄まる★
 *    ＝「実Excel と 違う」には ★2つ★ 在る：
 *       ①★合わせては いけない★（ここに 書く。★違う事が 正しい★）
 *       ②★まだ 直せて いない★（★本物の 負け★。ここには 書かない）
 *    ⇒ 数を 出す 時は この 2つを ★必ず 分ける★
 *      （2026-09-14 指示役1：「721/724 では 3件 劣化する ように 見える。実際は 1件」）
 *
 *  ★見張り★ … `tests/formula-yosoku.test.mjs` が
 *    「出して いない 物が 書いて いないか」「訳が 短すぎないか」を 押します。
 */
export const 別扱い = {
  'INFO': '★場の 事を 返す 物★＝実Excel は「Excel の 版」「開いている ブックの 数」を 返す。'
    + ' Exally が 同じ 数を 名乗ったら ★嘘★に なる。'
    + ' ⇒ うちの 版・うちの シートの 数を 返す（別の 試験で 1つずつ 押す）'
    + ' ／ 2026-09-07 司さん「全部やって」の 時に 決めた。'
    + ' 実測で 合わないのは `INFO("release")`（実Excel 16.0）と'
    + ' `INFO("osversion")`（実Excel Windows (64-bit) NT 10.00）の ★2本★。'
    + ' 残り（numfile／recalc／system ほか）は ★実Excel と 同じ★。',
};
