/* ribbon-scope.js — ★どこまで 作るか（対象外・出さない）の 正本★ 2026-08-30
 *
 *  ★なぜ ここに 1か所で 置くか★
 *    前は scripts/ribbon-bind.mjs の 中だけに 在った＝★画面からは 見えなかった★。
 *    その結果 ★「作らないと 決めた物」にも「これから」と 出ていた★
 *    （2026-08-30 監査役が 絵を 見て 見つけた）。
 *    ★守れない 約束を 見せない★ ために、bind も 画面も ★この1本を 見る★。
 *
 *  ★3通りに 分ける（監査役の 決め・2026-08-30）★
 *    (a) これから 作る          … ここに 載っていない物 → 画面は「これから」
 *    (b) Excelに 在るが 付けない … 対象外[] → 画面は ★「付けません」＋理由1行★
 *    (c) そもそも 押す物では ない … 出さない[] → ★組ごと 画面に 出さない★
 *
 *  ★黙って 分母から 引かない★＝(b)(c) とも ★理由を 書いた物だけ★。
 */
(function (root, factory) {
  if (typeof module === 'object' && module.exports) module.exports = factory();
  else root.RibbonScope = factory();
}(typeof self !== 'undefined' ? self : this, function () {
  'use strict';

  /* ══ (b) ★Excelに 在るが うちでは 付けない★（理由つき）════════ */
  var 対象外 = {
    'ホーム|アドイン|アドイン':                 'アドインの 仕組みが 無い',
    'ホーム|Claude|Claude':                     '別売りの 足し物です',
    'ヘルプ|モバイル|Excel モバイル アプリの入手': 'Microsoftの 宣伝です',
    /* ★開発タブ★＝うちは ★VBAを 動かさない（読むだけ）★ という 決まりなので
       「記録する」「危険の設定」は そもそも 要らない。★理由を 書いて 分母から 引く★ */
    '開発|アドイン|アドイン':           'アドインの 仕組みが 無い',
    '開発|アドイン|Excel アドイン...':  'アドインの 仕組みが 無い',
    '開発|アドイン|COM アドイン...':    'Windows専用の 仕組みです',
    '開発|コード|マクロのセキュリティ': 'VBAを 動かさないので 要りません',
    '開発|コード|マクロの記録...':      'VBAを 動かしません（レシピで 代えます）',
    '開発|コード|相対参照で記録':       'VBAを 動かしません（レシピで 代えます）',
    '開発|コントロール|デザイン モード': 'VBAの 部品の 仕組みです',
    '開発|コントロール|コードの表示':    'VBAの 部品の 仕組みです',
    '開発|コントロール|ダイアログの実行': 'VBAの 部品の 仕組みです',
    /* ★自動化タブの Microsoft 固有★ */
    '自動化|Office スクリプト ギャラリー|Office スクリプト': 'Microsoftの クラウドの 物（レシピで 代えます）',
    '自動化|Office スクリプト|Office スクリプト':            'Microsoftの クラウドの 物（レシピで 代えます）',
    /* ★ヘルプ|ヘルプ★＝2026-08-30 に 測り直して 見つけた（前の正本に 無かった）
       Microsoftの サービスへ つなぐ 物なので うちでは 付けない。 */
    'ヘルプ|ヘルプ|ヘルプ':                   'Microsoftの 窓口です',
    'ヘルプ|ヘルプ|サポートへのお問い合わせ...': 'Microsoftの 窓口です',
    'ヘルプ|ヘルプ|フィードバック':            'Microsoftへ 送る 物です',
    'ヘルプ|ヘルプ|トレーニングを表示':        'Microsoftの 教材です',
    'ヘルプ|ヘルプ|新機能ガイド':              'Microsoftの 案内です'
  };

  /* ══ (c) ★そもそも Excelの「押す物」では ない★＝★組ごと 出さない★════ */
  var 出さない = {
    '描画|元に戻す': 'Excelの ★状態表示★（押す物では ありません）'
    /* ※ Sheet1|ヘルプ ・ Sheet1|モバイル は ★ここに 書かない★。
       2026-08-30 に ★正本（docs/excel-ribbon-flat.tsv）から Sheet1 を 落とした★ので
       ここに 残すと ★使われない 許可★に なる（次の 本物を 見逃す）。
       理由は 下の 対象外タブ に 残してある。 */
  };

  /* ★Sheet1 は タブ そのものが 幻★
     2026-08-30 に 測り直して 中身を 押さえた＝
     「シートの追加／標準／改ページ プレビュー／右スクロール／拡大／縮小」＝
     ★下の シート見出しと 状態バー★だった。 */
  var 対象外タブ = { 'Sheet1': '下の シート見出しの 写り込み（Excelの リボンの タブでは ない）' };

  /** (b) か？ 理由を 返す（違えば null） */
  function 付けない理由(t, g, p) {
    return 対象外[t + '|' + g + '|' + p] || null;
  }
  /** (c) か？ 理由を 返す（違えば null） */
  function 出さない理由(t, g) {
    return 出さない[t + '|' + g] || null;
  }
  /** 分母から 引く物か（(b)(c) と 幻のタブ） */
  function 分母の外か(t, g, p) {
    return !!(対象外タブ[t] || 対象外[t + '|' + g + '|' + p] || 出さない[t + '|' + g]);
  }

  return {
    対象外: 対象外, 出さない: 出さない, 対象外タブ: 対象外タブ,
    付けない理由: 付けない理由, 出さない理由: 出さない理由, 分母の外か: 分母の外か
  };
}));
