# ★★FILTERXML ── 残り 4本／原因は 2つだけ★★（2026-09-18）

> ★★【この 紙の ㋑は 取り下げられました】★★（2026-09-18・Exally1 が 測り直した）
> ★`name()` は ★台の 欠陥では ありません★★
> ＝★jsdom が `name()` を 持って いない だけ★です
>   jsdom ........... `//*[name()='b']` -> `#VALUE!`
>   ★本物（WebKit）★ -> ★["1","2","3"]★（★読めます★）
> ＝`lib/formula-filterxml.js` は ★自分で XPath を 作って いません★
>   ＝`document.evaluate`（★ブラウザの 本物★）に そのまま 渡して います
> ⇒★★この 紙の ②㋑ と ④の「`name()` 1本」は 無効です★★
> ⇒★★繋ぐと 落ちる 物は ★1本★（`=LAMBDA(x,x*2)(3)` だけ）★★
> ⇒★本文は 直して いません★＝★測った 記録だから★／★日付で 読んで ください★
>
> ★★これで 今日 2回目です★★
>   1回目 ... `osu-dai-dake.mjs` が node で 押して FILTERXML 37本 全部 `#VALUE!`
>            （★道具に `DOMParser` が 無い★）... ★私（経営者1）が 取り下げた★
>   2回目 ... ★これ★（★jsdom に `name()` が 無い★）... ★Exally1 が 取り下げた★
> ⇒★★「台が 違う」と 言う 前に ★測り道具が その 式を 押せるか★を 見る★★
>
> ★本物で 押す 道具★ `docs/measured/xpath-name-honmono-de-osu.mjs`

> ★経営者1 が 測り、Exally1 が 書きます★
> ★★私（経営者1）は 台の 中身を 1字も 直して いません★★

---

## ０★★先に 取り下げ★★

```
  ★私は さっき「FILTERXML は 台の 欠陥 22本」と 出しました★
  ★★取り下げます★★
  ★訳★ ... `docs/measured/osu-dai-dake.mjs` は ★node で 台を 押して いました★
     `FILTERXML` は 台が ★道具が 無ければ 計算しない★ 作りです
       （`lib/shiki-tsunagi.js` ...「★半分 合う 答えを 出さない★」＝★正しい 作り★）
     node には `DOMParser` / `XPathResult` が ★在りません★
     ⇒★37本 全部 `#VALUE!`★ に なって いました
  ★直し★ ... jsdom の 窓から `Tsu.XML道具を渡す(...)`
     （★jsdom が 無ければ `exit 3`★＝★飛ばしません★）
  ⇒★★渡したら 33 / 37 でした★★
```

---

## ①★★実測（★紙 37本 全部★）★★

```
  ★紙★ `docs/measured/kansuu46/golden-filterxml-2026-09-07.tsv`
  ★台★ `lib/shiki-hyou.js` の 板に 打って 字を 読む（★jsdom を 渡した★）
  ★正★ 実Excel

  ★★合った 33 / 37★★ ／ ★外れ 4本★
```

★★合わない 4本（★全部★）★★

```
  ①ROWS(FILTERXML(X,"//e/text()"))    実Excel ★#VALUE!★ ／台 ★1★
  ②ROWS(FILTERXML(X,"//zzz"))         実Excel ★#VALUE!★ ／台 ★1★
  ③ROWS(FILTERXML(X,"count(//b)"))    実Excel ★#VALUE!★ ／台 ★1★
  ④FILTERXML(X,"//*[name()='b']")     実Excel ★1★      ／台 ★#VALUE!★
    ＋ROWS(同上)                        実Excel ★3★      ／台 ★1★
```

★X ＝ `<r><b>1</b><b>2</b><b>3</b><c id='x'>A</c><c id='y'>B</c><d><e>ne</e></d></r>`★

---

## ②★★原因は 2つだけです★★

### ㋐★★`ROWS` が 誤りを 通して いません★★（①②③）

```
  ★`FILTERXML` は 正しく `#VALUE!` を 返して います★（3本とも ○）
  ★でも `ROWS(#VALUE!)` が ★1★ を 返します★
  ⇒★★これは `FILTERXML` の 話では ありません★★
  ⇒★★`ROWS` が 誤りを 受け取ったら その 誤りを 返す★★ べきです
  ★裏取り★ ... 誤りで ない 時の `ROWS` は 全部 合って います
     `ROWS(FILTERXML(X,"//b"))` → 3 ○
     `ROWS(FILTERXML(X,"//c"))` → 2 ○
     `ROWS(FILTERXML(X,"//c/@id"))` → 2 ○
     `ROWS(FILTERXML(X,"//b[position()>1]"))` → 2 ○
  ⇒★溢れの 本数は 数えられて います★＝★誤りの 時だけ★
  ★★`COLUMNS` も 同じか は 見て いません★★（紙に 誤りの 例が 在りません）
```

### ㋑★★`name()` を 使った 道筋が 読めません★★（④）

```
  `//*[name()='b']` ... 実Excel ★1★（＋`ROWS` は ★3★）／台 ★#VALUE!★
  ★他の 道筋は 全部 読めて います★
     `//b` `//b[1]` `//b[2]` `//b[last()]` `//b[position()>1]`
     `//c` `//c[@id='y']` `//c/@id` `/r/d/e` `//d/*`
  ⇒★★`position()` と `last()` は 読めて `name()` だけ 読めません★★
  ⇒★関数の 名前の 表に `name()` が 入って いない 見込み★
  ★★当てて いません★★（★私は 中身を 読んで いません★）
```

---

## ③★★見て いない 事★★

```
  ・★紙は 1枚（37本）だけ★です＝★他の XML・他の 道筋は 見て いません★
  ・★溢れの 2つ目 以降の 値★は 見て いません（★`ROWS` の 数だけ★）
  ・★`COLUMNS` の 誤り★は 紙に 例が 在りません
  ・★画面（`book.html`）では 押して いません★
    ＝★台に jsdom を 渡した 姿★です
    ⇒★窓の 中なら `self.DOMParser` が 在るので ★同じか もっと 良い★ はずですが
      ★測って いません★
```

---

## ④★★これが 繋ぐ 前の 関門の 全部です★★

```
  ★繋ぐと 落ちる 物 ＝ ★6本★（1,872本を 1本ずつ 突き合わせた）
    `ROWS(FILTERXML(...))` ... ★4本★  ←★㋐★
    `FILTERXML(//*[name()...])` ... ★1本★  ←★㋑★
    `=LAMBDA(x,x*2)(3)` ......... ★1本★  ←★棚の 1件★
  ★繋ぐと 増える 物 ＝ 62本★
  ⇒★★㋐と ㋑と 棚の 1件を 直せば 繋いでも 1本も 落ちません★★
```

---

## ⑤★★おまけ（★決めが 要ります／私は 決めません★）★★

```
  `=LET(x,1,ISOMITTED(x))` ... 台 ★`FALSE`★ ／実Excel ★`False`★
  ⇒★★数は 同じ・字の 大小だけ★★
  ⇒★実Excel の `.Value2` は `False`／★画面は `FALSE`★★
  ⇒★★紙が どちらを 写して いるかで 変わります★★
  ⇒★これは 欠陥では なく ★どちらに 揃えるかの 決め★です★
  ⇒★★決めるのは 私では ありません★★
```
