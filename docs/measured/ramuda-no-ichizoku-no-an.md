# ★★ラムダの 一族 8個 — 何が 要るか（★書き始める 前の 1枚★）★★

2026-09-18 ／ ★★これは「決め」では ありません★★＝★形を 見せるだけ★

> ★なぜ 先に 1枚か★ … 経営者1「★大きい 物ほど 先に 形を 見せる★」
> ★なぜ 大きいか★ … ★★これは「関数を 1個 書く」では ありません★★
> 　　＝★式の 形の 台（評価の 仕組み）を 1つ 足す★仕事です。

---

## ① 何を 書くか（8個）

```
  LAMBDA   … 名前を 受け取る 式を 作る
  LET      … 名前に 値を 付けて 後ろで 使う
  MAP      … 並びの 1つずつに 当てる
  REDUCE   … 並びを 畳んで 1つに する
  SCAN     … 畳む 途中を 全部 出す
  BYROW    … 行ごとに 当てる
  BYCOL    … 列ごとに 当てる
  MAKEARRAY… 行数×列数 の 表を 作る
```

★紙（実Excel の 実測）★ `docs/measured/golden-oddf-to-46ko-2026-09-16.tsv`
```
  =BYCOL(A1:B2,LAMBDA(c,SUM(c)))      → 3
  =BYROW(A1:B2,LAMBDA(r,SUM(r)))      → 3
  =MAP(A1:A5,LAMBDA(x,x*2))           → 2
  =REDUCE(0,A1:A5,LAMBDA(a,b,a+b))    → 15
  =SCAN(0,A1:A5,LAMBDA(a,b,a+b))      → 1
  =MAKEARRAY(2,2,LAMBDA(r,c,r*c))     → 1
  ★LAMBDA と LET は 紙に 在りません★（★46個の 紙に 行が 無い★）
```
★★1関数 1行しか 在りません★★／★出て いるのは ★溢れの 左上★です★
　＝`=MAP(A1:A5,…)` の 2 は ★5つ 出た うちの 先頭★

---

## ② ★今 どこで 止まるか（★測りました★）★

### ㋐ 形に する（読む）… ★★1つだけ 通りません★★

```
  =LET(x,1,x+1)                  … ★通る★
  =MAP(A1:A5,LAMBDA(x,x*2))      … ★通る★（LAMBDA も 「呼」の 木に なる）
  ★=LAMBDA(x,x*2)(3)★           … ★★通りません★★
       「★終わりまで 読めて いない★（8 / 11）」
       ＝★`式(引数)` の 形（後ろに 括弧を 付けて 呼ぶ）を 読めません★
```

### ㋑ 計算する … ★★2つ 足りません★★

`lib/shiki-hyou.js` の `歩く()` を 読みました。

```
  ① ★名前の 台が 在りません★
       case '名' … TRUE / FALSE / ★マスの 名前★ の 3つだけ
                   それ以外は ★#NAME?★
       ⇒`LET(x,1,x+1)` の `x` は ★今 #NAME? に なります★

  ② ★引数が 先に 計算されます★
       case '呼' … `木.引数.map(引数にする)` ⇒ ★中身を 全部 計算してから★ 関数へ
       ⇒`LAMBDA(x,x*2)` の `x*2` も ★その場で 計算されます★（⇒ #NAME?）
       ⇒`LET(x,1,x+1)` の `x+1` も ★x が 決まる 前に★ 計算されます
```

⇒★★この 2つは ★どちらも 今の 8個 以外には 効きません★★★
　＝★他の 421個は 引数を 先に 計算して よい★

---

## ③ ★要る 物（★3つ★）★

### ㋐ ★名前の 台（束縛）★
```
  `歩く(木, 手, 通り道)` に ★もう 1つ 持たせる★ … `名の台`
    { x: <値>, 親: <外側の 名の台> }
  case '名' で ★先に 名の台を 見る★（★無ければ 今まで通り★）
  ★外を 先に 見ない★＝★マスの 名前より 名の台が 先★
    ⇒`=LET(A1,5,A1)` は ★5★の はず（★未測定★）
```

### ㋑ ★先に 計算しない 関数（特別な 形）★
```
  `関.呼ぶ` の 前に ★名前で 分ける★
    LAMBDA / LET … ★木の まま★ 受け取る
    それ以外 ……… 今まで通り（★1行も 触らない★）
  ★置き場★ … `lib/shiki-hyou.js`（★評価の 仕組みなので 板の 側★）
    ＝★`lib/shiki-kansuu.js` には 置けません★（★木を 知らない★）
```

### ㋒ ★ラムダという 値★
```
  { 型: 'ラムダ', 名たち: ['x'], 体: <木>, 名の台: <作られた 時の 台> }
  ★作られた 時の 台を 一緒に 持つ★＝★閉じ込め★
    ⇒`=LET(a,2,MAP(A1:A5,LAMBDA(x,x*a)))` が 動く（★未測定★）
  ★MAP など 6個は この 値を 受け取って 呼ぶだけ★
    ⇒★6個は `lib/shiki-kansuu.js` に 書けます★（★呼ぶ 口を 所 に 足せば★）
```

---

## ④ ★段取り（★1つずつ★）★

```
  ⑴★名の台★ を 足す（★`LET` だけ 先に★）
     ⇒`=LET(x,1,x+1)` が 2 に なる
     ⇒★他の 421個が 1本も 減らない事を 総なめで 見る★
  ⑵★ラムダの 値★ ＋ `LAMBDA` を 足す
     ⇒`=LAMBDA(x,x*2)` は ★単体では 何に なるか★＝★実Excel に 聞く★（#CALC! の はず）
  ⑶★MAP / BYROW / BYCOL / MAKEARRAY★（★当てるだけ★）
  ⑷★REDUCE / SCAN★（★畳む★）
  ⑸★`式(引数)` の 形★（`=LAMBDA(x,x*2)(3)`）… ★読む 側を 直す★
     ⇒★これだけ 別の 台（`lib/shiki-katachi.js`）です★
```

---

## ⑤ ★★聞かないと 決まらない 事（★9枠目に 積みます★）★★

```
  ㋐`=LAMBDA(x,x*2)` を ★そのまま マスに 打つと 何に なるか★
  ㋑`=LET(A1,5,A1)` … ★名の台と マスの 名前の どちらが 勝つか★
  ㋒`=MAP(A1:A5,LAMBDA(x,x*2))` の ★溢れの 形★（5行1列か）
  ㋓`=REDUCE` の 引数の 順（最初の 値／並び／ラムダ）
  ㋔`=SCAN(0,A1:A5,…)` の ★出る 形★（★0 を 含むか＝5個か 6個か★）
  ㋕`=BYROW(A1:B2,LAMBDA(r,SUM(r)))` が 3 ＝★行ごと★の 裏取り
     （★列ごとなら 4 の はず★＝A1+A2 ＝ 1+2 ＝ 3 ／ A1+B1 ＝ 1+2 ＝ 3
       ⇒★この 材料では 行でも 列でも 3★＝★分かれません★
       ⇒★★分かれる 材料で 聞き直す★★）
  ㋖引数の 数が 合わない 時（#VALUE! か #N/A か）
  ㋗★再び 自分を 呼ぶ ラムダ★（`=LAMBDA(n,IF(n=0,1,n*me(n-1)))`）は 受けるか
```

★★㋕は 今 気づきました★★
　＝★紙の 1行は 在るのに ★行と 列を 見分けられない 材料★です★
　＝★これを 見落として いたら「BYROW が 合った」と 言って いました★
　⇒★★「紙が 在る」は「確かめられた」では ありません★★

---

## ⑥ ★★私が まだ 言えない 事★★

```
  ・★速さは 測って いません★（木を 何度も 歩く＝MAP は 並びの 数だけ）
  ・★輪（循環）の 見つけ方★ … `通り道` は マスの 名前で 見て います
      ⇒★ラムダが 自分を 呼ぶ 時は それでは 止まりません★
      ⇒★深さの 上限★が 要るかも しれません（★未測定★）
  ・★溢れ（土台⑤）の 形★ … MAP は ★表を 返します★
      ⇒★板が 溢れを 置ける 事は 確かめて あります★が
        ★ラムダから 返った 表で 測って いません★
```

---

> ★★決めて ほしい 事★★
> ⑴この 段取り（④の ⑴〜⑸）で よいか
> ⑵⑤の 8つを ★9枠目で 聞いて よいか★（★枠は 6秒です★）
> ⑶★⑸（読む 側を 直す）は 後回しで よいか★
> 　＝`=LAMBDA(x,x*2)(3)` は ★お客さんが めったに 書きません★
> 　＝但し ★書けない事を 名指しで 残します★
