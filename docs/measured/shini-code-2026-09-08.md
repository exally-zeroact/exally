# ★JS層の 死にコード 12か所を 消した★（2026-09-08）

## ★死にコードとは★

`exally-formula.js` の `_jsComputeFormula`（＝JS層）には ★入口に 関所★が 在る。

```js
if(!_jsSet[_fnBase]) return null;   // JS非対象 → HFへ
```

`_jsSet` に 載っていない 関数は ★下の 段に 1度も 届かない★。
前に HyperFormula の プラグインへ 移した 関数の ★書き残し★が そのまま 残っていた。

## ★★なぜ 消したか（★数え間違いの 元★）★★

私も 指示役も ★「JS層は 38個」★と 数えて 話していた。
本当は ★26個★（12個は 死にコード）。

⇒★★死にコードは 動かないだけでは なく、★人が 数を 間違える★★

司さん「★使わん物は 消せ★」

## ★消した 12か所（★字読みで 決めず 12個とも 押した★）★

| 関数 | 押した 式 | JS層 | エンジンの 答え |
|---|---|---|---|
| MODE | `=MODE(A1:A5)` | ★届かない★ | `#N/A` |
| TRIMMEAN | `=TRIMMEAN(A1:A5,0.2)` | ★届かない★ | `3` |
| PERCENTRANK | `=PERCENTRANK(A1:A5,3)` | ★届かない★ | `0.5` |
| KURT | `=KURT(A1:A5)` | ★届かない★ | `-1.1999999999999993` |
| INTERCEPT | `=INTERCEPT(B1:B5,A1:A5)` | ★届かない★ | `0` |
| FORECAST | `=FORECAST(3,B1:B5,A1:A5)` | ★届かない★ | `6` |
| IRR | `=IRR(A1:A5)` | ★届かない★ | `4151205518922.167` |
| XLOOKUP | `=XLOOKUP(3,A1:A5,B1:B5)` | ★届かない★ | `6` |
| GESTEP | `=GESTEP(5,3)` | ★届かない★ | `1` |
| PERMUT | `=PERMUT(5,2)` | ★届かない★ | `20` |
| PERMUTATIONA | `=PERMUTATIONA(5,2)` | ★届かない★ | `25` |
| MDETERM | `=MDETERM(C1:D2)` | ★届かない★ | `-2` |

★★JS層に 届いた … 0個 ／ 12個★★
⇒ 消しても ★お客さんの 答えは 1文字も 変わらない★（消した 後に もう1回 押して 同じ 答えを 確かめた）

土台 … `A1:A5 = 1,2,3,4,5` ／ `B1:B5 = 2,4,6,8,10` ／ `C1:D2 = 1,2,3,4`

## ★消した 物★

- 上の ★12段★（コメント1行 ＋ `var m◯◯=fOrig.match(...)` ＋ `if(m◯◯)…` ＝ 3行ずつ ＝ ★36行★）
- `_jsXlookup`（★定義と 出し口★）
  ⇒ 消す 段からしか 呼ばれていない（他の ファイル・試験からも ★0件★と 数えた）

## ★★消さなかった 物（★1つずつ 中身を 見た★）★★

下請け 11個 … `_jsMode` `_jsTrimmean` `_jsPercentrank` `_jsKurt` `_jsIntercept`
`_jsForecast` `_jsIrr` `_jsGestep` `_jsPermut` `_jsPermutationa` `_jsMdeterm`

★他から 呼ばれている★ので 消していない。名指しで 数えた:

| 下請け | どこから 呼ばれるか |
|---|---|
| `_jsIntercept` | `_jsForecast` `_jsLinest` `_jsLogest` `_jsTrend` ／ プラグイン登録 |
| `_jsMode` | `AGGREGATE` の `case 13` ／ プラグイン登録 `EX.MODE` `EX.MODESNGL` |
| `_jsMdeterm` | `_jsMinverse` ／ プラグイン登録 |
| 他 8個 | プラグイン登録 |

⇒★★『段が 死んでいる』と『下請けが 死んでいる』は ★別★★

## ★★止め方＝コメントでは なく 見張り★★

コメントは「★また 書き戻される★」を 止められない。

`tests/js-sou-sekisho.test.mjs`

- ★関所を 通らない 段が 1つでも 在ったら 赤★
- ★逆向き★＝関所に 居るのに 段が 無い 関数が 在っても 赤
- ★消した 物が 本当に 消えているか★（`_jsXlookup` が 0個）
- 自己試験 … ★関所に 無い 段を 1つ 足すと 赤★／★関所が 読めない 時に 黙って 緑に しない★

## ★コードには 1行だけ★

★関数の 名前を 1つも 書いていない★
⇒ 名前を 残すと『その コードが 在るか』を ★字で 数えられなく なる★
（今日 実際に 踏んだ … 見張りの 説明に 探す 字を 書いて 自分で 通ってしまった）

## ★★落とさない（★次の 仕事★）★★

★★消した 12個は ★12個 とも★ 金の紙に 1本も 無い★★

`MODE` `TRIMMEAN` `PERCENTRANK` `KURT` `INTERCEPT` `FORECAST`
`IRR` **`XLOOKUP`** `GESTEP` `PERMUT` `PERMUTATIONA` `MDETERM`

★★私は 一度「4個」と 書きました＝★間違い★★★
  ⇒ 訳 … 打った 命令が `for f in MODE MODE.SNGL IRR KURT INTERCEPT` で
    ★12個 中 5個しか 見ていなかった★
  ⇒★規則の 話では なく ★見る 範囲を 数えなかった★だけ★
  ⇒★★決まり … ★for に 名前を 手で 並べたら その場で 個数を 数えて 書く★★
    （12個の はずが 5個＝★並べた 時点で 気づけた★）

⇒★死にコードを 消すと「JS層の 24個を 押す」にも 入らない★
⇒★★どちらの 計画からも こぼれる★★（指示役 2026-09-08）
⇒★★『24個を 押す』の 時に この 12個も 一緒に 押す＝★36個★★
⇒★★押す 順は ★XLOOKUP を 先頭★★
  （★お客さんが 一番 使う 関数の 1つ／1本も 確かめていない★）

★見立て（★まだ 測っていない★）★
- `=MODE(A1:A5)` → `#N/A`（1〜5 に 同じ 数が 無いので 実Excel も #N/A の はず）
- `=IRR(A1:A5)` → `4151205518922.167`（★桁が おかしい★）

⇒★その 時に 実Excel と 突き合わせれば 答えが 出る★
