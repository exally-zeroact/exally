# ★JS層の 36個を 実Excel に 聞いた★（2026-09-08）

## ★なぜ 測ったか★

★JS層（`_jsComputeFormula`）が 本当に 受ける 26個の うち、
答えを 確かめたのは ★2個だけ★★（ISOMITTED・LINEST）。

さらに ★死にコードだった 12個は 12個とも 金の紙に 0本★。

```
26 − 2 ＋ 12 ＝ ★36個★
```

★見た 3個の うち 2個が 壊れていた★（`N`・`DATESTRING`）＝**当たりの 密度が 高い**。

⇒ 司さんの「答え 確かめて」の ★続き★（金の紙が 届いていなかった 所）

## ★紙★

| ファイル | 中身 |
|---|---|
| `toru-jssou36.ps1` | ★実Excel に 聞く 道具★（人が 決めるのは 引数の 形の 候補だけ） |
| `golden-jssou36-2026-09-08.tsv` | ★実Excel の 答え★（32個・103本） |
| `awase-jssou36.mjs` | Exally と 突き合わせる（★本番と 同じ 入口と 出口★） |
| `awase-jssou36-2026-09-08.tsv` | 判定（式 / 判定 / うちの 答え） |
| `j36-wakeru.mjs` | ★原因ごとに 分ける★ |

★土台★ `A1:A5=1..5` / `B1:B5=2,4,6,8,10` / `C1=0 C2=3 C3=0.1 C4=0.2 C5=1` / `F1:I3=台` / `K1:K2=条件`
（★C1 に 0 を 置いた★＝`||` が 0 を 落とすかを 見る為）

## ★数★

```
 実Excel に 聞いた … ★32個で 呼び方が 見つかり 103本★
   ★呼び方が 見つからない 4個★ … BINOM DGET ★XIRR IRR★
   ⇒★『動く』でも『動かない』でも ない＝★まだ★★
   ⇒★★IRR・XIRR は ★お金の 関数★＝呼び方を 変えて 測り直す★★（指示役 2026-09-08）

 突き合わせ
   合った …………………………… ★62本★
   ★★違う（黙って 間違った 数）… 14本（関数 5個）★★
   ★こちらだけ 誤り ……………… 17本（関数 2個）★
   名前が 通らない ……………… 10本（関数 5個）
```

## ★★見つかった 物★★

### ★★①`DATESTRING` は ★元号★を 返す 関数だった★★

```
=DATESTRING(2)   実Excel ★明治33年01月02日★  ／ うち 1900/1/1
=DATESTRING(5)   実Excel ★明治33年01月05日★  ／ うち 1900/1/4
=DATESTRING(A1)  実Excel ★明治33年01月01日★  ／ うち 1899/12/31
=DATESTRING(C1)  実Excel ★明治33年01月00日★  ／ うち ★NaN/NaN/NaN★
```

⇒ うちは ★西暦の 形★を 返している＝**出る 字が 丸ごと 違う**
⇒ ★日本の お客さんが 使う 関数★

★★直す 時の 決め（指示役 2026-09-08）★★
- ★元号の 境目を ★頭で 書かない★＝必ず ★実Excel に 打たせて 測る★★
  （明治→大正／大正→昭和／昭和→平成／平成→令和 の ★境目の 日・前日・翌日★）
- ★元年を「1年」と 書くか「元年」と 書くかも ★実Excel の 字を 見る★★
- ★訳★ … ★暦は 法で 決まっている＝覚えている 事を 書いては いけない★
- ★★『字の 形』と『1日の ずれ』は ★別の 直し★＝分けて 数える★★
  （`=DATESTRING(2)` が `01月02日`／うちは `1900/1/1`）
- `C1=0` の `NaN/NaN/NaN` は ★`||` が 0 を 落とす★件

### ★★②`MODE`・`KURT` は 引数が 2つ以上で 全部 `#N/A`★★（17本）

```
=MODE(A1:A5,B1:B5)  実Excel ★2★     ／ うち ★#N/A★
=KURT(A1:A5,2)      実Excel ★-0.86★ ／ うち ★#N/A★
```

⇒ 今日 消した 死にコードの 段は ★引数 1つの 形しか 見ていなかった★
  （`/^MODE(?:\.SNGL|\.MULT)?\s*\(([A-Z]+\d+:[A-Z]+\d+)\)$/`）
⇒★消した 判断は 正しい（段は 届いていなかった）★
⇒★★でも ★エンジン側が 引数 2つ以上を 受けていない★＝★別の 穴★★
⇒★★『消したから 終わり』では ない＝押したから 見つかった★★

### ★③その他★

| 関数 | 本数 | 中身 |
|---|---|---|
| `N` | 5 | `=N(5)` → `0`（実Excel は `5`）… ★生の 字を そのまま 渡している★ |
| `FREQUENCY` | 5 | `#NAME?`（実Excel は 答える） |
| `DSTDEV` `KURT` `FORECAST` | 4 | ★15桁の 丸め★（IM系と 同じ 家） |

## ★★直す 順（指示役 2026-09-08 が 決めた）★★

```
 ★1★ ★N★ ……………… ★狭くて 黙って 違う＝一番 先★（直す 所は 1か所）
 ★2★ ★DATESTRING★ … 出る 字が 丸ごと 違う／★1つの 関数に 閉じる★
 ★3★ MODE・KURT（引数 2つ以上）
 ★4★ FREQUENCY
 ★5★ ★15桁の 丸め★ …★engine の 数 全部が 通る 道＝★一番 広い★ので 最後★
 ★別★ IRR・XIRR・DGET・BINOM を ★呼び方を 変えて 測り直す★（★N の 次に 上げる★）
```

⇒★★『片づく 数が 多いほど 後ろ』では なく『★触る 幅が 広いほど 後ろ★』★★
