# ★★LENB ／ LEFTB ／ RIGHTB ／ MIDB ── ★測って いない 事★★★（2026-09-18）

> ★書きました★（台 400 → ★404★／台に 無い 56 → ★52★）
> ★紙の 12本 とも 合って います★
> ★★但し 見込みは 11 / 12 ── ★1本 外しました★★★

---

## ①★★★外した 1本（★消しません★）★★★

```
  `=LEN(MIDB(全角ア&"A",2,2))`
    ★私の 見込み★ … ★1★
    ★実Excel★ ……… ★★2★★
  ★訳★ … バイトは ［ア＝1〜2］［A＝3］。
        2バイト目から 2バイト 取ると ★ア の 後ろ半分★ ＋ ★A★
        ⇒★★後ろ半分は 空白 1文字に なる★★ ⇒ 空白 ＋ A ＝ ★2文字★
  ⇒★見張り `tests/lenb.test.mjs` に ★名指しで★ 入れました★
    （★次に 誰かが 1文字に したら 赤★）
```

---

## ②★★測って 合わせた 分（★12 / 12★）★★

```
  =LENB("ABC")                → ★3★   半角 1バイト
  =LENB(全角ア)                → ★2★   ★全角 2バイト★
  =LENB(半角ｱ)                 → ★1★   ★半角カナは 1バイト★
  =LENB(全角ア&"A")            → ★3★
  =LENB("")                    → ★0★
  =LEN(LEFTB(全角ア,1))        → ★1★
  =UNICODE(LEFTB(全角ア,1))    → ★★32（空白）★★
  =LEN(LEFTB(全角ア,2))        → ★1★
  =LEN(LEFTB(全角ア&"A",3))    → ★2★
  =LEN(RIGHTB(全角ア&"A",1))   → ★1★
  =LEN(MIDB(全角ア&"A",1,2))   → ★1★
  =LEN(MIDB(全角ア&"A",2,2))   → ★2★
```

### ★決まり★

```
  ★★2バイトの 字を ★半分で 切ると 空白 1文字に なる★★★（★実測★）
```

---

## ③★★測って いない 事（★名指し★）★★

```
  ⑴★★1バイトか 2バイトかを 測ったのは ★3つだけ★★★
     半角A（1）／全角ア（2）／半角ｱ（1）
     ★うちの 決め★ … ASCII（〜U+007F）と 半角カナ（U+FF61〜U+FF9F）が ★1バイト★
                    それ以外が ★2バイト★
     ★★漢字・ひらがな・全角記号は 測って いません★★
       ⇒★Shift-JIS に 無い 字（絵文字・一部の 漢字）で どう なるかも 分かりません★

  ⑵★サロゲート（絵文字）★ … ★2バイトに して います／未測定★
  ⑶★長さを 省いた 時★（`=LEFTB("ABC")`）… ★1 に して います／未測定★
  ⑷★負の 長さ★／★始まりが バイト数を 越える★
  ⑸★`MIDB` で 後ろ半分 だけ 取った 時の 字★
     ⇒★うちは 空白★／★紙は「2文字」までしか 言って いません★
     ⇒★1文字目が 空白（32）かは ★LEFTB で 測って あります★★
       （`=UNICODE(LEFTB(全角ア,1))` → 32）
     ⇒★MIDB の 方は ★裏返しで 書いて います★★
```

---

## ④★★次の 枠で 聞く 式（★14本★）★★

```
  ★1バイトか 2バイトか（★一番 大事★）★
    =LENB(UNICHAR(26085))        ★漢字 日★
    =LENB(UNICHAR(12354))        ★ひらがな あ★（★済み＝ア だけ 測った★）… ★ひらがな は 未測定★
    =LENB(UNICHAR(65281))        ★全角！★
    =LENB(UNICHAR(8212))         ★ダッシュ —★
    =LENB(UNICHAR(9731))         ★雪だるま ☃★
  ★MIDB の 後ろ半分★
    =UNICODE(MIDB(UNICHAR(12354)&"A",2,2))   ★★1文字目は 空白か★★
    =UNICODE(MIDB(UNICHAR(12354)&"A",2,1))   ★後ろ半分 1バイトだけ★
    =LEN(MIDB(UNICHAR(12354)&"A",2,1))
  ★境目★
    =LEFTB("ABC")                ★長さを 省く★
    =LEFTB("ABC",-1)             ★負★
    =MIDB("ABC",9,2)             ★始まりが 外★
    =LENB(TRUE)                  ★真偽★
    =LENB(123)                   ★数★
    =RIGHTB(UNICHAR(12354),1)    ★後ろ半分★
  ★見込みは ★聞く 前に★ 書きます★
```

---

## ⑤★★お客さんに 何が 変わるか★★

```
  ★前★ … `=LENB("あ")` は 画面で ★#NAME?★
  ★後★ … ★2★
  ★但し★ … ★台は まだ お客さんの 道に 出て いません★（`shiki-*` は 0本）
```
