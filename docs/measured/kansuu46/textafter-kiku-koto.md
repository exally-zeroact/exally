# ★★TEXTAFTER ／ TEXTBEFORE ── ★測って いない 事★★★（2026-09-18）

> ★書きました★（台 404 → ★406★／台に 無い 52 → ★50★）
> ★紙の 12本 とも 合って います★
> ★★但し 見込みは 9 / 12 ── ★3本 外しました★★★

---

## ①★★★外した 3本（★2本は 逆でした★・消しません）★★★

```
  ★①`=TEXTAFTER("a-B-c","b")`★
     ★私の 見込み★ … "-c"（★大小を 見ない と 思って いた★）
     ★実Excel★ ……… ★★#N/A★★
  ★②`=TEXTAFTER("a-B-c","b",1,1)`★
     ★私の 見込み★ … #N/A
     ★実Excel★ ……… ★★"-c"★★
  ⇒★★つまり 逆でした★★
     ★既定（4つ目を 省く）＝★大小を 見る★★
     ★`1` ＝★大小を 見ない★★
  ⇒★★SEARCH（見ない）と FIND（見る）の 感じとは 別です★★
     ＝★「TEXT〜 は SEARCH の 仲間」と 思うと 必ず 逆に します★
  ⇒★見張りの ★一番 上の 3本★に しました★

  ★③`=TEXTBEFORE("abc","")`★
     ★私の 見込み★ … #VALUE!（★「見込み 無し」と 書きました★）
     ★実Excel★ ……… ★★空の 字★★（★誤りに なりません★）
```

---

## ②★★測って 合わせた 分（★12 / 12★）★★

```
  =TEXTAFTER("a-b-c","-")      → "b-c"
  =TEXTAFTER("a-b-c","-",2)    → "c"
  =TEXTAFTER("a-b-c","-",-1)   → "c"      ★後ろから 1つ目★
  =TEXTAFTER("a-b-c","x")      → #N/A
  =TEXTAFTER("a-b-c","-",5)    → #N/A     ★行き過ぎ★
  =TEXTBEFORE("a-b-c","-")     → "a"
  =TEXTBEFORE("a-b-c","-",2)   → "a-b"
  =TEXTBEFORE("a-b-c","-",-1)  → "a-b"
  =TEXTBEFORE("a-b-c","x")     → #N/A
  ＋上の 3本
```

---

## ③★★測って いない 事（★名指し★）★★

```
  ⑴★5つ目（match_end）★ … ★うちは 受けて いません★
     ＝`=TEXTAFTER("abc","x",1,0,1)` の ような 形
  ⑵★6つ目（if_not_found）★ … ★うちは 受けて いません★
     ＝★#N/A の 代わりに 好きな 字を 返す★
     ⇒★★この 2つは 引数を 渡されても 黙って 無視します★★
        （★測って いない ので 動きを 決められません★）
  ⑶★区切りが 並び（四角）★ … ★TEXTJOIN は 並びを 受けます★（実測済み）
     ⇒★TEXTAFTER も 受けるか ★測って いません★★
  ⑷★空の 字を 探す TEXTAFTER★（`=TEXTAFTER("abc","")`）
     ⇒★うちは 本文 まるごと★／★測って いません★
  ⑸★0 番目★（`=TEXTAFTER("a-b","-",0)`）⇒★うちは #VALUE!／未測定★
  ⑹★重なる 区切り★（`=TEXTAFTER("aaa","aa",2)`）⇒★うちは 重なりも 数える／未測定★
  ⑺★本文が 空★／★数や 真偽を 渡した 時★
```

---

## ④★★次の 枠で 聞く 式（★14本★）★★

```
  ★受けて いない 2つ（★一番 大事★）★
    =TEXTAFTER("abc","x",1,0,1)          ★match_end＝1★
    =TEXTBEFORE("abc","x",1,0,1)
    =TEXTAFTER("abc","x",1,0,0,UNICHAR(12394)&UNICHAR(12375))   ★if_not_found★
    =TEXTBEFORE("abc","x",1,0,0,UNICHAR(12394)&UNICHAR(12375))
    =TEXTAFTER("a-b","-",9,0,0,UNICHAR(12394)&UNICHAR(12375))   ★行き過ぎ ＋ if_not_found★

★★なぜ `UNICHAR(12394)&UNICHAR(12375)`（＝「なし」）と 書くか★★（2026-09-18）
  ★式の 中に 日本語の 字を 直に 書くと、
    `.ps1` → PowerShell → COM → 実 Excel の 間で ★化ける 所が 在ります★★
  ⇒★化けた 時、答えが 違うのか 字が 化けたのかが ★見分けられません★★
  ⇒★だから 字は ★番号から 作ります★★（★式は ASCII だけに なる★）
  ★門★ … `toru-kansuu-8kaime.ps1` が ★式に ASCII の 外の 字が 在れば 止まります★
  ★これで 聞く 事は 変わりません★ … ★if_not_found に 渡した 物が そのまま 返るか★
  ★境目★
    =TEXTAFTER("abc","")                 ★空の 区切り（後ろ）★
    =TEXTAFTER("a-b","-",0)              ★0 番目★
    =TEXTAFTER("aaa","aa",2)             ★重なる 区切り★
    =TEXTBEFORE("aaa","aa",2)
    =TEXTAFTER("","x")                   ★本文が 空★
    =TEXTAFTER(123,"2")                  ★数を 渡す★
    =TEXTAFTER(TRUE,"R")                 ★真偽を 渡す★
    =TEXTAFTER("a-b-c",{"-";"b"})        ★区切りが 並び★
    =TEXTAFTER("a-B-c","B")              ★大小が 逆の 組★（★念の為 もう 1本★）
  ★見込みは ★聞く 前に★ 書きます★
```

---

## ⑤★★お客さんに 何が 変わるか★★

```
  ★前★ … `=TEXTAFTER("a-b","-")` は 画面で ★#NAME?★
  ★後★ … ★"b"★
  ★但し★ … ★台は まだ お客さんの 道に 出て いません★（`shiki-*` は 0本）
```
