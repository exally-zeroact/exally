# ★★ASC ／ DBCS ── ★測って いない 事★★★（2026-09-18）

> ★書きました★（台 398 → ★400★／台に 無い 58 → ★56★）
> ★紙の 22行 とも 合って います★（`golden-kansuu-7kaime-2026-09-18.tsv`）
> ★★22行は 聞く 前の 見込み どおり＝当て推量 0★★

---

## ①★★★一番 大事な 事 ── ★字の 表を 記憶で 書いて いません★★★★

```
  ★半角カナの 表は 63字 在ります★（U+FF61〜U+FF9F）
  ★記憶で 書くと 必ず どこか 間違えます★
    （2026-09-08 … 役所の 続柄コードを ★12個 並べて 12個とも 違いました★）
  ⇒★★`String.normalize('NFKC' ／ 'NFKD')` を 引きます★★
     ＝★node（と ブラウザ）が 持って いる Unicode の 表★
     ＝★★私の 記憶では なく 機械の 表★★
  ★見張り★ … ★63字を 端から 端まで 通して「1字も 落ちない」を 見ます★
     （`DBCS` で 全角に して `ASC` で 戻す ⇒ ★元に 戻るか★）
     ⇒★★63 / 63★★
```

---

## ②★★測って 合わせた 分（★22 / 22★・11字）★★

```
  ★ASC（全角 → 半角）★
    全角Ａ(65313) → ★65★ ／1文字      全角１(65297) → ★49★ ／1文字
    全角ア(12450) → ★65393★ ／1文字
    ★全角ガ(12460) → ★65398★ ／★2文字★★（★濁点が 分かれる★）
    ★全角空白(12288) → ★32★ ／1文字★
    全角、(12289) → ★65380★ ／1文字
  ★DBCS（半角 → 全角）★
    半角A(65) → ★65313★      半角1(49) → ★65297★
    半角ｱ(65393) → ★12450★
    ★半角濁点(65438) → ★12443★★  半角空白(32) → ★12288★
```

### ★★実測が 1か所 教えて くれました★★

```
  ★DBCS(半角濁点 U+FF9E)★
    `NFKC` … ★U+3099（★結合用★の 濁点）★
    ★実Excel … U+309B（★単独の ゛★）★
  ⇒★★ここだけ 別に して います★★（★測った から 書けました★）
  ⇒★機械の 表を 引いて いても ★実測が 要る 所が 在る★★
```

---

## ③★★測って いない 事（★名指し★）★★

```
  ⑴★★全角ガ の 2文字目★★
     ★紙は「2文字」までしか 言って いません★（1文字目の コードだけ 在る）
     ⇒★うちは U+FF9E（半角濁点）に して います★
     ⇒★見張りも「何か 1文字 在る」だけを 見て います★（★中身を 決めつけません★）

  ⑵★★`ASC(U+309B)`（全角の ゛ を 半角に）★★
     ⇒★★実測の 裏返しで 書きました★★
        （`DBCS(U+FF9E)` → U+309B は 測った ⇒ その 逆を 置いた）
     ★訳★ … `NFKD` は U+309B を ★空白 ＋ 結合濁点★に ばらすので
             ★往復で 戻らなく なります★
     ⇒★★「記憶で 埋めるより 実測済みの 隣に 合わせる」（09-18 の 学び）★★
     ⇒★★但し これ 自体は 測って いません★★

  ⑶★63字の うち 測ったのは ★4字★★（ア・ガ・、・空白）
     ⇒残り 59字は ★機械の 表を 信じて います★
  ⑷★半濁点（パ）★／★ー（長音）★／★ヴ★／★ひらがな★
  ⑸★全角の 記号（！＃＄…）★ … ＋0xFEE0 の 引き算で 出して います（★ASCII 2字だけ 測った★）
  ⑹★2字 以上の 字／サロゲート★
```

---

## ④★★次の 枠で 聞く 式（★14本★）★★

```
  ★裏返しで 書いた 所（★一番 大事★）★
    =UNICODE(ASC(UNICHAR(12539)))      ★全角の ・★
    =UNICODE(ASC(UNICHAR(12443)))      ★★全角の ゛ ⇒ 半角に なるか★★
    =LEN(ASC(UNICHAR(12443)))          ★★1文字か 2文字か★★
    =UNICODE(MID(ASC(UNICHAR(12460)),2,1))  ★★ガ の 2文字目★★
  ★表の 端★
    =UNICODE(ASC(UNICHAR(12497)))      ★全角パ（半濁点）★
    =LEN(ASC(UNICHAR(12497)))
    =UNICODE(ASC(UNICHAR(12540)))      ★長音 ー★
    =UNICODE(ASC(UNICHAR(12532)))      ★ヴ★
    =LEN(ASC(UNICHAR(12532)))
    =UNICODE(ASC(UNICHAR(12354)))      ★ひらがな あ（★変わらない はず★）★
  ★記号★
    =UNICODE(ASC(UNICHAR(65281)))      ★全角！★
    =UNICODE(ASC(UNICHAR(65509)))      ★全角￥★
    =UNICODE(DBCS(UNICHAR(92)))        ★半角 \ ★
    =UNICODE(DBCS(UNICHAR(126)))       ★半角 ~ ★
  ★見込みは ★聞く 前に★ 書きます★
```

---

## ⑤★★お客さんに 何が 変わるか★★

```
  ★前★ … `=ASC("Ａ")` は 画面で ★#NAME?★
  ★後★ … ★"A"★
  ★但し★ … ★台は まだ お客さんの 道に 出て いません★（`shiki-*` は 0本）
```
