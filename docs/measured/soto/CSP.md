# CSP — 今 どこへ 出ているか／入れたら 何が 止まるか（2026-09-07）

> ★本番には 入れていません★（指示役の 注文＝「入れないで ください／一覧だけ」）
> ★どちらも 本物の Chrome で 撃って 数えた★（頭で 考えた 一覧では ない）

---

## ★★まず 私の 間違いを 2つ 書きます★★

```
 ★間違い①★ 字で 探した 一覧を そのまま 出しかけた
   ⇒ repo を 字で 探すと `schemas.openxmlformats.org` が ★33回★ 出る
   ⇒ でも これは ★XML の 名札★＝★1回も 取りに 行かない★
   ⇒★実測 … 字で 15件／うち ★12件は 1度も 出ていない★★

 ★間違い②★ ブラウザで 撃ったら ★18の 相手★が 出て、それを そのまま 書きかけた
   ⇒ 中を 見たら ★大半が Chrome 自身の 用事★
      （辞書の 取り寄せ・時計合わせ・拡張の 更新・自動入力の 問い合わせ）
   ⇒ 直し … ★何も 無い ページを 先に 3回 開いて 引く★
             ＋★Chrome の 用事を 止める 印を 付ける★
   ⇒★★18 → 5★★（2回 撃って 同じ 数）
```

---

## ★① 今 画面が 外へ 出ている 先 … ★5つ★★

★`book.html` も `hub.html` も 同じ 5つ★

| 相手 | 何を 取っているか | 止めたら どう なるか |
|---|---|---|
| `exally.vercel.app` | ★自分★（画面・部品） | — |
| `fonts.googleapis.com` | 字体の 決まり書き（CSS） | ★字体が 変わる★ |
| `fonts.gstatic.com` | 字体そのもの（woff2） | ★字体が 変わる★ |
| `cdn.jsdelivr.net` | 倉庫の 部品（supabase-js 2.112.4） | ★倉庫に 入れなくなる★ |
| `a.nel.cloudflare.com` | ★jsdelivr が 出させている★（失敗の 報せ） | ★何も 変わらない★ |

```
 ★`a.nel.cloudflare.com` の 出どころを 確かめた★
   `curl -I https://cdn.jsdelivr.net/...` の 返しに
   `Nel:` と `Report-To:` が 在り、行き先が まさに その 住所
 ⇒★★うちが 書いた 物では なく、jsdelivr から 借りている 事の 副産物★★
 ⇒★借り先を 1つ 足すと ★知らない 相手が 1つ 増える★★
```

★この 測りが 見ていない 物（隠さず 書く）★
```
 ★押さないと 出ない 先★
   ・`/api/claude`（AIに 聞く）… ★同じ 出どころ★なので 'self' に 入る
   ・`tnfwipbgfgjaymlszeid.supabase.co`（倉庫）… ★開いた だけでは 出ない★
   ・「Web から」の 窓 … ★お客さんが 打った 住所★
 ⇒★開いた だけ＝半分です★
```

---

## ★② CSP を 入れたら 何が 止まるか（実測）★

★手元に 同じ 画面を 立てて、そこにだけ CSP を 付けた★
★止まった 物は ★ブラウザ自身に 言わせた★（securitypolicyviolation）★

### ★(あ) 中に 書いた 物を 許さない 形 … ★864件 止まる★★

```
  default-src 'self'
  script-src  'self' https://cdn.jsdelivr.net
  style-src   'self' https://fonts.googleapis.com
  font-src    'self' https://fonts.gstatic.com
  img-src     'self' data: blob:
  connect-src 'self' https://*.supabase.co
```

| 止まった 物 | 数 |
|---|---|
| `style-src-attr`（`style="…"` を 直に 書いている 所） | ★855回★ |
| `style-src-elem`（`<style>` の 中） | 4回 |
| `script-src-elem`（`<script>` の 中） | 3回 |
| わざと 止めた 2つ（物差しの 確かめ） | 2回 |

```
 ⇒★★この 形は 使えません★★
 ⇒ 理由＝`book.html` は ★`style="…"` を 855か所 直に 書いている★
 ⇒★直すと したら 855か所＝別の 大仕事★
```

### ★(い) 中に 書いた 物を 許す 形 … ★止まるのは 2件だけ★★

```
  script-src  'self' 'unsafe-inline' 'unsafe-eval' https://cdn.jsdelivr.net
  style-src   'self' 'unsafe-inline' https://fonts.googleapis.com
  （他は 同じ）
```

```
 ★止まった 2件は ★どちらも 私が わざと 撃った 物★★
   ・許していない 相手の 絵     … img-src が 止めた
   ・許していない 相手への 取得 … connect-src が 止めた
 ⇒★★画面の 物は 1つも 止まっていません★★
```

---

## ★③ この 2つの 数字が 意味する 事★

```
 ★★(い) の 形なら 今日でも 入ります★★（画面は 1つも 止まらない）
 ★★しかも それでも「どこへでも 出る」は 止まります★★
   ⇒ 実測で ★connect-src と img-src が わざとの 弾を 止めた★
 ⇒★★⑧（外へ 出る 関数）に とって これは 二重の 錠に なります★★
    ・私が 書き間違えても ブラウザが 止める
    ・お客さんが 打った 住所も 止まる
```

```
 ★でも ★ただ では 入りません★★
 ★(い) を 入れると ★今 在る「Web から」の 窓が 止まります★★
   ⇒ `Webを取りに行く()`（book.html 3499行）は
     ★お客さんが 打った 住所へ 取りに 行く★＝connect-src に 引っ掛かる
   ⇒★実測で その 撃ち方が 止まる 事を 確かめた★
 ⇒★★入れるなら『Web から』を どうするかを 先に 決める★★
   道は 2つ
     ・★うちの サーバを 通す★（行き先を うちが 決める）
     ・★その 窓を 畳む★
 ⇒★★どちらも お客さんに 見える 変更＝司さんの 決めが 要る★★
```

```
 ★`'unsafe-eval'` を 残している 訳★
   ⇒★ここを 書いた 時は ★まだ 測っていませんでした★★
   ⇒★その後 測りました ⇒ ★④へ★（答え＝★要ります／止まるのは 1本だけ★）★
```

---

## ★測った 物★

| ファイル | 何を したか |
|---|---|
| `ima-doko-e-dete-iru.mjs` | 本物の Chrome の 通信の 記録から 相手を 数えた（Chrome 自身の 用事を 引いた） |
| `ima-doko-e-dete-iru-book.txt` / `-hub.txt` | その 結果（2枚とも 5つ） |
| `csp-ireta-ra-nani-ga-tomaru.mjs` / `.txt` | 手元に 同じ 画面を 立てて CSP を ★3通り★ 試し、★式を 4通り 押した★ |

---

## ★④ `'unsafe-eval'` は 要るか … ★要ります★（実測）★

> 指示役の 注文 … 「★入れない・数だけ★／『入れたら 何が 止まるか』を 穴なく」

★測り方★ … (い) から `'unsafe-eval'` ★だけ★ 外して、★式を 4通り 押した★

| 押した 道 | ゆるい 形 | `'unsafe-eval'` を 外した 形 |
|---|---|---|
| 本道 `setCellFormula`（`=SUM(1,2)*3`） | ★出来た（9）★ | ★出来た（9）★ |
| 本道 `LEN`（`=LEN("あいう")`） | ★出来た（3）★ | ★出来た（3）★ |
| JS層 `CONVERT`（`=CONVERT(1,"m","cm")`） | ★出来た（100）★ | ★出来た（100）★ |
| ★JS層 `evalFormula`（`=1+2*3`）★ | ★出来た（7）★ | ★★出来ない★★ |

```
 ★★止まるのは 1本だけです★★
 ★場所★ … `book.html` `evalFormula()`
   var result = Function('"use strict";return ('+expr+')')();
 ⇒★この 1行の 為だけに `'unsafe-eval'` が 要ります★
```

### ★★一番 大事なのは「止まり方」です★★

```
 ★止まっても ★赤も 出ないし 画面も 壊れません★★
 ⇒ `evalFormula` は 中で つかまえて ★式を そのまま 返す／`#ERR` を 返す★
 ⇒★★お客さんから 見ると「なんか 答えが 出ない」だけ★★
 ⇒★★『入れたら 動かなく なった』に 気づけない 形です★★
```

### ★この 道は いつ 通るか★

```
 `setCellFormula()` の 中で ★HyperFormula が 赤（#…）を 返した 時だけ★ 逃げ道として 呼ばれる
 ⇒★普段は 通りません★
 ⇒★でも「HF が 苦手な 式」を ★拾っている★ のが この 道★
 ⇒★外すと その 分だけ ★黙って 答えが 出なくなる★★
```

### ★選べる 道（決めるのは 司さん）★

```
 ★あ. `'unsafe-eval'` を 付ける★ … 今日 入れられる／★錠が 1つ ゆるい★
 ★い. `evalFormula` の Function を 自前の 計算に 直す★
    ⇒ 相手は ★数と ＋−×÷（ ）だけ★（正規表現で そう 縛ってある）
    ⇒★小さな 計算機を 1つ 書けば 足りる＝★外せる★★
    ⇒★でも これは「今 やらない」と 決めた 話の 外＝別の 用件★
 ⇒★私は 数えただけ／どちらも やっていません★
```

---

## ★⑤ 消してはいけない 数（指示役の 注文）★

```
 ★★855 ／ style-src-attr★★
 ⇒ `book.html` が `style="…"` を ★直に 書いている 所の 数★
 ⇒★これは 捨てる 数字では ありません★
 ⇒★★『いつか きれいな CSP に する』時の 見積り★★
 ⇒★今 やらない＝正しい／でも ★数は 消さない★★
```
