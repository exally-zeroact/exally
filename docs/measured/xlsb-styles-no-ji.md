# ★`.xlsb` の `xl/styles.bin` の 字★（85）（2026-09-21）

★これは 仕様書の 写しでは ありません★。
★実Excel に 作らせた 包みを ほどいて 測った 物★です。

## ★なぜ★
Exally1「★`xl/styles.bin` の 読み解きが 一番 大きい 残りです★」
⇒飾りが `.xlsb` で 画面まで 届くのは 今 8/13。残り 5つは 全部 ここです。

## ★どうやって 測ったか（★当て推量で 名前を 付けない★）★
1. ★飾りを 1つだけ★ 変えた 包みを 5本 作る（`tsukuru-xlsb-kazari-1tsu-zutsu.ps1`）
   ... 何も しない / 字だけ / 塗りだけ / 線だけ / 書式だけ
   ★2マスは わざと 別の 飾り★（同じだと 記録が 1本しか 増えず 割れない）
2. `xl/styles.bin` を 記録の 並びに ほどく（`yomu-xlsb-no-kiroku.mjs`）
3. ★どの 番号が 増えたか★で 名前を 決める

★名前は ここから 出しました★＝「そう 呼ばれているから」では ありません。

### ★読み方が 合って いる 証し★
記録を 順に 食べて ★余り 0 バイト★で 末尾に 着きます（ずれて いたら 余ります）。

## ★① どの 番号が 何か（★実測★）★

| 番号 | 何も しない | 字 | 塗り | 線 | 書式 | ⇒ 名前 |
|---|---|---|---|---|---|---|
| 43 | 2 | ★4★ | 2 | 2 | 2 | ★字（フォント）★ |
| 44 | 0 | 0 | 0 | 0 | ★2★ | ★書式の 字（numFmt）★ |
| 45 | 2 | 2 | ★5★ | 2 | 2 | ★塗り★ |
| 46 | 1 | 1 | 1 | ★3★ | 1 | ★線（罫線）★ |
| 47 | 2 | ★4★ | ★5★ | ★4★ | ★4★ | ★マスの 飾りの 組★ |
| 615/616 | 0 | 0 | 0 | 0 | ★1★ | ★書式の 入れ物★（要る時だけ 出る） |

★動かなかった 番号★ 33個（＝飾りと 関わりが 無い）。

## ★② 入れ物（★名乗りと 実際を 突き合わせ済★）★

| 入れ物 | 閉じ | 中身 |
|---|---|---|
| 615 | 616 | 44（書式の 字） |
| 611 | 612 | 43（字）... ★名乗りの 外に `37`〜`38` の 塊が 入ります★ |
| 603 | 604 | 45（塗り） |
| 613 | 614 | 46（線） |
| 626 | 627 | 47 ... ★下敷きの 組（cellStyleXfs）★ |
| 617 | 618 | 47 ... ★マスが 指す 組（cellXfs）★ |
| 619 | 620 | 48 ... ★名前つきの 飾り★（ここにも `37`〜`38` が 入ります） |

★`611` と `619` は 名乗りと 実際が 合いません★＝`37`〜`38` の 塊が 中身の 外に 居ます。
★番号 35 は 長さ4ですが 入れ物では ありません★（名乗ると 3586本＝★数では ない★）。

## ★③ `47`（マスの 飾りの 組・16バイト）の 中身（★実測★）★

| バイト | 中身 | 証し |
|---|---|---|
| 0–1 | ★下敷きの 組★ | 下敷き側は `ffff` |
| 2–3 | ★書式の 番号★ | 書式だけ 変えた 包みで 176 / 177 に なり、`44` の 頭の 番号と ★一致★ |
| 4–5 | ★字の 番号★ | 字だけ 変えた 包みで 1 / 2 ＝`611` の 中の ★場所★ |
| 6–7 | ★塗りの 番号★ | 塗りだけ 変えた 包みで 2 / 3 ＝`603` の 中の ★場所★ |
| 8–9 | ★線の 番号★ | 線だけ 変えた 包みで 1 / 2 ＝`613` の 中の ★場所★ |
| 10–11 | ★未測定★ | どの 包みでも 0 |
| 12–13 | ★未測定★ | どの 包みでも `08 10` |
| 14–15 | ★どこを 変えたかの 印★ | 書式=0x0001 / 字=0x0002 / 線=0x0008 / 塗り=0x0010 |

★0x0004 と 0x0020 は 測って いません★（揃え・保護を 変えた 包みを まだ 作って いません）。

★字の 番号で 1回 間違えかけました★
　中身の 突き合わせだと 「増えた 字は 2番目と 3番目」に 見えます。
　★場所で 数え直すと 1・2・3番目が 変わって いました★
　＝★Excel は 元から 在る 字も 書き直します★。
　⇒★中身が 同じでも 別の 場所は 別の 物★（突き合わせでは 同じ 物が 消える）。

## ★④ `45`（塗り）の 中身（★3色で 割りました★）★

```
01 00 00 00 | 05 ff 00 00 | ff 00 00 | ff | 03 40 00 00 ...
^^^^^^^^^^^   ^^                       ^^   ^^
塗り方         色の 種類                 ?    次の 色（後ろの 色）
                          ^^^^^^^^
                          ★R G B★
```

| バイト | 中身 | 証し |
|---|---|---|
| 0–3 | ★塗り方★ | 0＝無し / 17＝細かい点 / ★1＝べた塗り★ |
| 4 | ★色の 種類★ | べた塗りの 3本 とも `05` |
| 8,9,10 | ★R G B★ | 赤・緑・青の 3本で ★ここだけ★ 変わる |
| 11 | ★`ff`★ | 3本とも 同じ |

★2色では 割れません★（違う バイトが 2つ 出るだけで 並び順が 決まらない）。
★3色目（青）で 8=R / 9=G / 10=B が 決まりました★。

## ★⑤ マスが 組を どう 指すか（`xl/worksheets/sheet1.bin`）★

★番号 2（12バイト）＝数の 入った マス★

★★2026-09-22 ── ★ここは 私が 間違えて いました★（Exally1 が 数え直して くれました）★★
　私は 「8-11 ＝数（double）」と 書きました。
　★12バイトに double（8バイト）は 入りません★。★8-11 は 4バイト★です。
　★正しくは RK（縮めた 数）★でした。

| バイト | 中身 | 証し |
|---|---|---|
| 0–3 | ★列★（0から） | A列で 0 |
| 4–? | ★組の 番号★ | 塗った 包みで 1 / 2 ＝`617` の 中の 場所と ★一致★ |
| 8–11 | ★RK（縮めた 数）★ | 下の 通り ★実物で 3 / 1 / 0.25 が 出ました★ |

★RK の 解き方（★実測で 合いました★）★
```
 下位2ビットが 印
   ビット0 ... 立って いたら ★100で 割る★
   ビット1 ... 立って いたら ★30ビットの 整数★／立って いなければ
              ★上位30ビットを double の 上半分★に して 下を 0 で 埋める
 実物（tests/fixtures/kazari-hiraku3.xlsb）
   A1 `00 00 08 40` ⇒ ★3★    （実Excel も 3）
   A2 `00 00 f0 3f` ⇒ ★1★    （実Excel も 1）
   A3 `00 00 d0 3f` ⇒ ★0.25★（実Excel も 0.25）
```
★私が なぜ 外したか★
　`00 00 f0 3f` を 見て ★double 1.0 の 上半分★だと 読み、
　★「double が 入って いる」と 書いて しまいました★。
　＝★長さを 数えて いません★（12 − 4 − 4 ＝ ★4★ しか 残らない）
　⇒★「それらしい 字」を 見たら 長さで 割る★。

★★組の 番号が 24ビットか 32ビットかは ★この 材料では 分かれません★★★
　Exally1 は 32ビットと 書き、私は 24ビットと 書きました。
　★実物の 8バイト目（byte7）は ★どのマスも 0★★ なので ★どちらでも 同じ 数★に なります。
　⇒★分かれる 材料を 作るまで 「どちらか」は 書きません★
　⇒★どちらで 読んでも 今の 実物では 同じ★（＝実害は 今の 所 ありません）

★番号 0（25バイト）＝行の 頭★ ... 0–3バイトが ★行★（0から）。

## ★測って いない 事（★ここを 「分かった」と 言わない★）★
- 字（`43`）・線（`46`）の 中の 場所 ... ★増える 番号までしか 見て いません★
- 色が ★テーマ色★の 時の 形（今 測ったのは ★直に 指定した 色★だけ）
- 文字の 入った マス（`sharedStrings.bin` を 指す 記録）
- 揃え・保護（`47` の 印 0x0004 / 0x0020）
- ★板が 2枚 以上★ の 時の 組の 番号（板ごとに 別か 共通か）

## ★使った 物★
- 作る ... `tsukuru-xlsb-kazari-1tsu-zutsu.ps1`（★飾りを 1つだけ 変えた 包み 5本★）
- ほどく ... `yomu-xlsb-no-kiroku.mjs`（★名前を 付けない★）
- ★どの Excel か★ ... 版 16.0 ／ build 20326
- ★どの 貝殻か★ ... PowerShell 5.1.26100.9278

## ★⑥ `43`（字・35バイト）の 中身（★実測・87★）★

材料 `tsukuru-xlsb-ji-to-sen-1tsu-zutsu.ps1`（★1マスに つき 1つだけ 変える★）

| バイト | 中身 | 証し |
|---|---|---|
| 0-1 | ★大きさ x 20★ | 既定 220＝11pt ／ 20pt に したら 400 |
| 2-3 | ★印★ | 太字=0x01 斜体=0x02 下線=0x04 取消線=0x08 が ★1つずつ★ 立った |
| 4-5 | ★太さ★ | 既定 400 ／ 太字で ★700★ |
| 8 | ★下線★ | 下線を 付けた 時だけ 0 ⇒ 1 |
| 9 | ★未確定★ | 既定 2 ／ ★太字・斜体で 3★ に なる（なぜかは 分からない） |
| 10 | ★字の 組★ | 既定 0x80（128）／ Arial で 0 |
| 12-19 | ★色（下の ⑧）★ | 赤に したら kind が 7 ⇒ 5 に なり R が ff |
| 20 | ★未確定★ | 既定 2 ／ Arial で 0 |
| 21-24 | ★名前の 字数★ | Arial で 5 |
| 25- | ★名前★ | UTF-16（`41 00 72 00 69 00 61 00 6c 00`＝Arial） |

## ★⑦ `46`（線・51バイト）の 中身（★実測・87★）★

★1バイト目★ ＋ ★10バイトの 塊 x 5★ ＝ 51。

| 塊 | 何の 辺か | 証し |
|---|---|---|
| 1-10 | ★上★ | 上だけ 打った マスで ここだけ 変わる |
| 11-20 | ★下★ | 下だけ 打った マスで ここだけ 変わる |
| 21-30 | ★左★ | 左だけ 打った マスで ここが 変わる |
| 31-40 | ★右★ | 右だけ 打った マスで ここだけ 変わる |
| 41-50 | ★未測定★ | 斜めは 打って いません |

★塊の 中身★ ... 頭2バイト ＝ ★線の 形★ ／ 残り 8バイト ＝ ★色（下の ⑧）★
  形 ... 1＝細い ／ 3＝破線 ／ ★5＝太い★（実測した 3つだけ）

## ★★⑧ 色は どこでも 同じ 8バイト★★（★3か所で 同じ 形★）

```
kind idx  ?  ?   R  G  B  A
 05   ff  00 00  ff 00 00 ff   ← 赤（直に 指定）
 03   40  00 00  00 00 00 ff   ← 線の 既定（自動）
 07   01  00 00  00 00 00 ff   ← 字の 既定（★テーマの 色★）
```

★塗り・字・線の 3か所 とも 同じ 形でした★。
kind ... ★3＝自動／5＝直に 指定／7＝テーマ★（★この 3つだけ 実測★）
⇒★テーマの 色は kind=7 で 見分けられます★（idx が テーマの 何番目か）。
★但し 「7番の テーマ色が 実際に 何色か」は まだ 測って いません★。

## ★★⑨ ★Excel は 線を 隣の マスへ 移します★★（★一番 大事★）

A5 に ★下の 線（太い）★を 打って 保存したのに、
★A5 の 組が 指す 線は ★空（0番）★★でした。
★太い 線は A6 の ★上★ に 入って いました★（形＝5）。
同じ 事が もう 1組（A6 の 赤い 下線 ⇒ A7 の 上に 赤で 入る）。

⇒★マスの 線を 「そのマスの 線の 記録」だけで 読むと ★線が 消えます★★
⇒★上の 辺は 1つ 上の マスの 下の 辺も 見る／左の 辺は 1つ 左の マスの 右の 辺も 見る★

★この せいで 私の 「太さ」の 測りは 失敗して います★
  ＝A5 の 記録が 空に なった ので ★太さの バイトを A5 からは 読めて いません★
  （太さは ★A6 の 上の 塊★ から 読めました＝形 5）

## ★★⑩ テーマの 色（kind=7）＝★色は 既に 入って います★★★（実測・88）

材料 `tsukuru-xlsb-tema-iro-1tsu-zutsu.ps1`（★1マスに テーマの 色を 1つだけ★）

| 包みの idx | 8バイト | 実際の 色 | Excel の テーマの 表 |
|---|---|---|---|
| 0 | `07 00 00 00 ff ff ff ff` | #ffffff | 2番 |
| 1 | `07 01 00 00 00 00 00 ff` | #000000 | 1番 |
| 2 | `07 02 00 00 e8 e8 e8 ff` | #e8e8e8 | 4番 |
| 3 | `07 03 00 00 0e 28 41 ff` | #0e2841 | 3番 |
| 4 | `07 04 00 00 15 60 82 ff` | #156082 | 5番 |
| 5 | `07 05 00 00 e9 71 32 ff` | #e97132 | 6番 |
| 6 | `07 06 00 00 19 6b 24 ff` | #196b24 | 7番 |
| 7 | `07 07 00 00 0f 9e d5 ff` | #0f9ed5 | 8番 |
| 8 | `07 08 00 00 a0 2b 93 ff` | #a02b93 | 9番 |
| 9 | `07 09 00 00 4e a7 2e ff` | #4ea72e | 10番 |

★★一番 大事★★
　★kind=7（テーマ）でも R G B の 3バイトに ★もう 実際の 色が 入って います★★
　⇒★テーマの 表を 引かなくても 色は 出せます★（`07` でも `05` でも 同じ 所を 読む）

★★但し ここまでしか 言えません★★
- ★実Excel が 書いた 包みで しか 測って いません★
  ＝他の 道具が 書いた 包みで R G B が 0 のままかは ★未測定★
  ⇒★`kind` を 見ずに R G B だけ 読む のは 危ない★（0 だと 黒に なる）
- ★濃さ（tint）を 付けた 色は 測って いません★（3-4バイト目が それかも しれない・未測定）
- テーマを ★別の テーマに 変えた 時★は 測って いません

★★ついでに 出た 事＝COM の 名前と 実際の 色は 入れ替わって います★★
　`Font.ThemeColor = 1`（名前は `Dark1`）⇒ ★実際は 白★
　`Font.ThemeColor = 2`（名前は `Light1`）⇒ ★実際は 黒★
　（3と4も 同じく 逆）
　⇒★COM の 名前を 信じて 明暗を 決めると 逆に なります★

## ★★⑪ 残って いた 未測定を 閉じました★★（実測・89）

材料 `tsukuru-xlsb-nokori-1tsu-zutsu.ps1`
★1行 おきに 置きました★＝⑨の 「隣へ 移る」に 巻き込まれない 為（移り先が 空の 行）。

### ★線の 太さは 「形」と ★同じ 2バイト★★
| 打った 太さ | 頭2バイトの 値 |
|---|---|
| 細い（既定） | 1 |
| 中くらい | 2 |
| 太い | 5 |
| 極細 | 7 |
| 破線（87で 測った 物） | 3 |
⇒★形と 太さは 別の 場所では ありません★＝1つの 数で 表します。

### ★斜めの 線★
- ★1バイト目★ が 0 ⇒ ★1★ に なる（斜めが 在る 印）
- ★41-50バイト★（5つ目の 塊）に 線が 入る
⇒★⑦の 「41-50 未測定」は 斜め でした★

### ★`47` の 12-13バイトは 「未確定」では ありませんでした★
| 打った 物 | 12-13 | 印（14-15） |
|---|---|---|
| 何も しない | `0810`（0x1008） | 0x0000 |
| ★真ん中 揃え★ | `0a10`（0x100a） | ★0x0004★ |
| ★鍵を 外す★ | `0800`（0x0008） | ★0x0020★ |
⇒★0x1000 の ビットが 「鍵が 掛かって いる」★（外すと 落ちる）
⇒★下の 方の ビットが 横の 揃え★（既定 8 ⇒ 真ん中で 10）
⇒★印 0x0004＝揃え ／ 0x0020＝保護★（★85で 未測定と 書いた 2つ★）

### ★文字の 入った マス★
★記録の 番号 7★（12バイト）... 0-3 列 ／ 4-6 組 ／ ★8-11 が 文字の 番号★
（数の マスは 番号 2）
★但し 文字が 1つしか 無い 包みで 測りました★＝★0以外の 番号は 見て いません★。

## ★★まだ 測って いない 物（2026-09-21 時点）★★
- ★他の 道具が 書いた 包み★（色の R G B が 0 のままか）
- ★濃さ（tint）を 付けた 色★
- ★板が 2枚 以上★ の 時に 組の 番号が 板ごとに 別か 共通か
- ★縦の 揃え・折り返し・回転★（12-13 の 残りの ビット）
- ★文字の 番号が 2つ 以上★ 在る 時

## ★★⑫ `.xlsx` と `.xlsb` で ★色の 書き方が 違います★★★（実測・90）

Exally1 の 問い「`.xlsx` の `styles.xml` は `<color theme="4"/>` と ★番号しか 書いて いない★のでは」
⇒★その通りでした★。★同じ 盤面を 2つの 形で 保存して 突き合わせました★。

| | 書いて ある 物 |
|---|---|
| `.xlsb`（`styles.bin`） | ★番号 ＋ 実際の R G B★（`07 04 00 00 15 60 82 ff`） |
| `.xlsx`（`styles.xml`） | ★番号だけ★（`<color theme="4"/>`） |

⇒★`.xlsx` は テーマの 表を 引かないと 色が 出ません★（書き分けが 要ります）

### ★`<color theme="n"/>` の n ⇒ `theme1.xml` の 何番目か（★実測★）★
| n | 実際の 色 | `theme1.xml` の 名 | 書いて ある 順 |
|---|---|---|---|
| 0 | #ffffff | lt1 | ★1番目★ |
| 1 | #000000 | dk1 | ★0番目★ |
| 2 | #e8e8e8 | lt2 | ★3番目★ |
| 3 | #0e2841 | dk2 | ★2番目★ |
| 4-9 | accent1-6 | accent1-6 | ★4-9番目（そのまま）★ |

⇒★0と1、2と3だけ 入れ替わって います★（★書いて ある 順に 数えると 白黒が 逆に なります★）
⇒`.xlsb` の idx も ★同じ 番号★でした（0=白 / 1=黒 / 2 / 3 ...）

### ★測って いない 事★
- ★濃さ（tint）★ ... `<color theme="4" tint="0.4"/>` の 形は 測って いません
- ★テーマを 別の テーマに 変えた 時★
- `hlink` / `folHlink`（10番・11番）は ★マスに 当てて いません★
