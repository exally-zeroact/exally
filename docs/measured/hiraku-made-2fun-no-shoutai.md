# ★開くまで 2分の 正体★（2026-09-25・監査役）

★司さん 09-24「遅すぎることないか？」への 答え★

---

## ★★答え★★

★ファイルを 読むのに 掛かって いたのでは ありません★。
★式を 計算する 所で ★同じ 事を 1億4千万回★ やり直して いました★。

★1回 覚えて おくだけで 59.5秒 → 32.1秒★（★答えは 1つも 変わりません★）。

---

## ★測った 材料★

```
 代行計算表2026.xlsb   400,087 バイト
 sha256 2af78a40451f6f68fd08849f6a82b1b3fc72ac082cebb4cf67b08f9def28fabc
 置き場 C:\Users\zeroa\kyukyu-0907\   ←★OneDrive の 外の 写し★
 ★なぜ 外の 写しか★ ... OneDrive の 中の 物は ★24回中 15回 断られます★（別紙）
 本の 大きさ ... ★板 15枚 ／ マス 35,760個 ／ 式 15,799個★
```

---

## ★①まず 「外から 観る」が 塞がって いました★（道具 113）

1秒ごとに 節目を 観る 道具を 書きました。結果:

```
   111,177 ms （+111,177）  台:XLSX
   111,177 ms （+      0）  台:XlsxIO
   ...（台 11本・板が 増えた・マスが 入った・画面に 色が 付いた）
   111,177 ms （+      0）  画面に 色が 付いた
```

★節目 14個が 全部 同じ 時刻・間隔 +0★。

★これは 「14個が 最後に 一気に 終わった」のでは ありません★。
★私の 問い合わせが 1回も 動けて いない★ ＝
★画面の 仕事台が 111秒 ずっと 1本の 処理で 塞がって いた★ という 事です。

⇒★段（読み込み → 飾り → 描く）では ありません★。★1本の 止まらない 計算★。
⇒★外から 観る 道具では これ以上 割れません★。

> ★★覚え書き★★
> ★「節目が 全部 同時刻・間隔 0」は 「速かった」では なく 「観られて いない」★。
> ⇒★1秒ごとの 問い合わせが 通らない＝仕事台が 塞がって いる★
> ⇒★外から 観るのを やめて 中に 記録させる★（CPU の 記録）

---

## ★★②どこが 重いか（CPU の 記録・道具 114）★★

★chromium の `Profiler`（2ms ごと）★。※★webkit に この 口は 在りません★。

### ファイル別

| 割合 | ファイル |
|---|---|
| ★87.4%★ | ★`lib/shiki-hyou.js`★ |
| 7.3% | (JS の 中) |
| 0.9% | `hyperformula.full.min.js` |
| 0.8% | `lib/shiki-kansuu.js` |
| ★0.6%★ | ★`lib/xlsx.full.min.js`★ ←★ファイルを 読む 所は これだけ★ |
| 0.5% | `lib/shiki-afure.js` |
| 0.5% | `lib/shiki-ita-awase.js` |
| 0.4% | `lib/shiki-keisan.js` |
| 0.3% | `book.html` |
| 0.2% | `lib/shiki-basho.js` |

### 中の 内訳（同じ 名が 何度も 出るのは ★別の 呼ばれ方★・合計）

| 割合 | 所 |
|---|---|
| ★34.5%★ | ★`名から番地` `lib/shiki-hyou.js:76`★ |
| ★19.7%★ | ★`読む` `lib/shiki-hyou.js:499`★ |
| 3.6% | `引数にする` `:324` |
| 2.6% | `直す` `:908` |

---

## ★★③何回 呼ばれて いるか（道具 115）★★

★割合だけでは 直し方が 決まりません★:
・★マスの 数だけ（N）★なら「1回を 速くする」話
・★2乗（N²）★なら「呼ぶ 回数を 減らす」話
⇒★数えました★（★配る 時だけ 数取りを 挟む／repo は 触らない★）

| 呼ばれた 物 | 回数 | ★`読む` の 何倍★ |
|---|---|---|
| ★`名から番地`★ | ★141,920,112★ | ★2.24倍★ |
| ★`番地から名`★ | ★127,853,182★ | ★2.02倍★ |
| `揃える` | 64,600,815 | 1.02倍 |
| ★`読む`★ | ★63,380,276★ | 1倍（★本当の 仕事★） |

### ★2乗では ありません★

```
 35,760² ＝ 1,278,777,600  ≫  実際の 63,380,276
 ⇒★「全部と 全部を 突き合わせて いる」のでは ありません★
 ⇒`読む` 6,338万回 ＝★式 15,799個 × 平均 4,012マスの 四角★＝★仕事その物★
 ⇒★重いのは 「読む 回数」では なく 「1回 読む 度の 字の 出し入れ」★
     1マス 読む 度に ... ★字を 2.02回 作り／正規表現で 2.24回 読み直し／揃えるを 1.02回★
     ＝★1マスに 5.3回★
```

### ★形（字を 読んだ 結果）★

```
 lib/shiki-hyou.js:494   並び.push(読む(番地から名(r, c, 板3), 通り道));   ←★番地 → 字★
 lib/shiki-hyou.js:500   function 読む(名, 通り道) {
 lib/shiki-hyou.js:501     var n = 揃える(名);
 lib/shiki-hyou.js:504     var ば板 = 名から番地(n);                       ←★字 → 番地（正規表現）★
 ⇒★四角を 読む たびに マス 1つ ずつ 字に して、その 字を すぐ 正規表現で 番地に 戻して います★
```

### ★★別々の 字は 53,365 種類 しか 在りません★★

```
 141,920,112 回 ÷ 53,365 種類 ＝★同じ 字を 平均 2,659回 読み直して います★
 ⇒★「呼ぶ 回数を 減らす」では なく 「覚えて おく」で 効きます★
 ⇒★覚えるのに 要る 場所は 53,365個だけ★
```

---

## ★★④「覚えれば 効く はず」を ★測って から★ 言う（道具 116）★★

★外して 測るまで 見立て★。★実際に 覚えさせて 秒を 測りました★（★各 3回・交互★）。

| 通り | ★中ほど★ | 一番速い | 一番遅い | 揺れ幅 | 物差し比 | 各回 |
|---|---|---|---|---|---|---|
| ⓪★そのまま★ | 59,471 | 55,979 | 76,919 | 20,940 | ★100%★ | 76919 / 59471 / 55979 |
| ①★`名から番地` を 覚える★ | ★32,078★ | 30,814 | 46,208 | 15,394 | ★54%★ | 46208 / 30814 / 32078 |
| ②★`番地から名` も 覚える★ | 37,206 | 34,830 | 47,446 | 12,616 | 63% | 47446 / 34830 / 37206 |

### ★揺れ幅を 物差しに する★

```
 ★一番 大きい 揺れ幅 ... 20,940 ms★
 ⇒★この 幅より 小さい 差を 「効いた」と 言っては いけません★

 ⓪ → ①   ★27,393 ms★   ★幅より 大きい＝差が 在ります★
 ⓪ → ②    22,265 ms     ★幅より 大きい＝差が 在ります★
 ① → ②    -5,128 ms     ★幅の 中＝差は 言えません★
                         ★但し 3回 とも ② の 方が 遅い★
                         （46,208→47,446 ／ 30,814→34,830 ／ 32,078→37,206）
                         ＝★向きは 揃って います★＝★②を 先に やる 理由は 在りません★
```

### ★★揺れ幅の 中身★★（★出鱈目な ばらつきでは ありません★）

```
 ★各 3回の 1回目が いつも 一番 遅い★（76,919 ／ 46,208 ／ 47,446）
 ⇒★幅 20.9秒の 大半は 「1回目が 遅い」★
 ⇒★前後を 比べる 時は 1回目を 物差しに しない★
```

### ★★答えは 9回 とも 1つも 違いません★★（★速くても 答えが 違えば 失格★）

| | マス | 式 | 誤 | 数 | 字 | 数の 足し算 |
|---|---|---|---|---|---|---|
| ⓪①②（各3回） | 35,760 | 15,799 | ★0★ | 3,857 | 31,903 | ★54241061.144673★ |

### ★配った 字（★そのまま 使えます・決めるのは 作り手★）★

```js
  var __覚1 = new Map();
  function 名から番地(名) {
    var k = String(名); var v = __覚1.get(k);
    if (v !== undefined) return v;
    v = __名から番地の元(k); __覚1.set(k, v); return v;
  }
  function __名から番地の元(名) {        /* ←★元の 中身は 1文字も 変えて いません★ */
```

### ★安全か（★機械で 確かめました★）★

```
 `名から番地` を 呼ぶ 所 ............ ★16か所★
 その 返り物を 書き換えて いる 所 .... ★0件★（`.板=` `.行=` `.列=` で 探して 0）
 ⇒★同じ 物を 返しても 誰も 壊しません★
 ⇒★道具 116 は 毎回 これを 確かめます★（0件で ない なら exit 5）
```

---

## ★★⑤★まだ 言えない 事★★★

```
 ・★秒数は 台で 違います★ ... chromium 59.5秒 ／ ★webkit 111秒★
     ＝★見たのは 「どこが 重いか（割合）」であって 秒数では ありません★
     ＝★webkit の 割合は 未測定★（記録の 口が 無い）
     ＝★お客さんの 台には webkit（iPhone の Safari）が 在ります★
 ・★①を 入れても まだ 32秒 です★
     ★残る 分の 行き先（★見立て・未測定★）★
       ・`読む` 6,338万回 そのもの（式 1個が 平均 4,012マスの 四角を 見る）
       ・`番地から名` 1億2,785万回 の 字作り
     ⇒★①を 入れた 後に もう一度 CPU の 記録を 取れば 次の 87.4% が 見えます★
 ・★「開いた 直後に そもそも 計算が 要るのか」は 未測定★
     ＝実Excel は ★保存済みの 値★を 持って います
     ＝★これは 直し方では なく 「どう 振る舞うか」の 決め＝司さんの 決めに 掛かります★
     ⇒★「速くする」は 答えを 変えない★ので ★先に ①を 決め切る★
 ・★覚えの 束を いつ 捨てるか は 未測定★
     ＝`名から番地` は ★板の 名前に 依らない 変換だけ★なので ★捨てなくて よい はず★
     ＝★但し これは 字を 読んだ 結果＝作り手が 確かめる★
 ・★53,365種類 は この 本の 数★＝★別の 本なら 別の 数★（上限を 付けるかは 作り手の 判断）
```

---

## ★★⑥外した 見込み（★捨てません★）★★

```
 ・★`Object.defineProperty` で 台を 包んで 観る★
     ⇒★画面が 落ちました★（`Target page, context or browser has been closed`）
 ・★2秒ごとに 重い 問い合わせを して 観る★
     ⇒★観る 事 自体で 2分が 300秒を 超えました★＝★測る 道具が 測る 物を 変えました★
 ・★これが「読み込みが 遅い」だと 思って いた★
     ⇒★読む 所は 0.6%★＝★外れ★
```

---

## ★道具★

| 番 | 道具 | 何を するか |
|---|---|---|
| 113 | `hakaru-2fun-no-uchiwake.mjs` | 1秒ごとに 節目を 観る（★塞がって いて 割れない事★が 分かった） |
| 114 | `hakaru-doko-ga-omoi-ka.mjs` | CPU の 記録（chromium・CDP `Profiler`） |
| 115 | `kazoeru-nankai-yondeiru-ka.mjs` | 回数と ★別々の 字の 種類★ を 数える |
| 116 | `hakaru-oboetara-hayaku-naru-ka.mjs` | ⓪①② を ★各 何回でも★ 交互に 測り ★揺れ幅★ と ★答えの 一致★ を 出す |

★113〜116 は repo の `lib/` を 1バイトも 触りません★（★配る 時だけ 字を 包みます★）。
★116 は 走らせる 前後で `lib/shiki-hyou.js` の sha256 を 突き合わせます★（違えば exit 7）。

★生の 出し★ ... `docs/measured/hiraku-made-2fun-no-shoutai.out.txt`
