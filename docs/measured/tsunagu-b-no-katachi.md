# ★★㋑ の 形（★書く 前に 見せる★）★★

2026-09-18 ／ ★★まだ 1文字も 書いて いません★★

---

## ① 今の 道（★実物を 読みました★）

★式を 計算する 所は 2か所★です。

```
  book.html:2611  function setCellFormula(sheet, r, c, v)   ... ★1マス 打った 時★
  book.html:2893  function recalcSheet(sheetIdx, data)      ... ★板ごと 計算し直す 時★
```

★どちらも 同じ 順★
```
  ⑴var jsResult = _jsComputeFormula(sheet, v);   ← ★JS層★
     if (jsResult !== null) return jsResult;
  ⑵convertFormula(v) -> hf.setCellContents(...) ← ★借り物★
     return _hfGetDisplay(...);
```

⇒★★今は ★JS層 -> 借り物★の 2段です★★

---

## ② ㋑で する 事（★3段に する★）

```
  ⑴JS層（`_jsComputeFormula`）
  ⑵★★台（`ShikiHyou`）★★   ←★ここを 足す★
  ⑶借り物（HyperFormula）
```

★台が `#NAME?` を 返したら ⑶へ 落とす★
　＝★ODD 503個など ★台が 知らない 名前★は 今まで通り 借り物が 答えます★
　＝★私の 門「台に 無いが 残って いる 間は...」は これが 在る から 通せます★

★★経営者1 の 実測（1,872本・紙の 上で 混ぜた）★★
```
  今（借り物だけ） ... 1,393
  ★台が 先★ ....... ★1,466（+73）★／★下がる 0本★
  借り物が 先 ...... 1,414（+21）
```
⇒★★「台が 先」で 書きます★★

---

## ③ ★★一番 大きい 引っ掛かり ── ★台は 板が 要ります★★★

```
  ★借り物★ ... `hf.setCellContents({sheet,row,col}, 値)` ＝★1マスずつ 入れられる★
  ★★台★★ ... `ShikiHyou.表()` に ★打つ★＝★板を 1つ 持たないと 式が マスを 読めません★
  ⇒★`=SUM(A1:A5)` を 台に 聞くには ★A1:A5 が 台の 板に 入って いないと いけません★
```

★手は 2つ★

| | やり方 | 良い 所 | ★悪い 所★ |
|---|---|---|---|
| ㋐ | ★板を 1つ 持ち、マスを 打つ たびに 台にも 打つ★ | 速い | ★打つ 所が 5か所＝★1つ 忘れると 黙って 古い 値★★ |
| ㋑ | ★聞く たびに `sheets[i].data` から 板を 作り直す★ | ★忘れる 所が 無い★ | ★マスの 数だけ 掛かる＝★実物 15,799式では 重い★★ |

★★私の 案★★ ... ★㋐（板を 持つ）／但し ★門を 先に 置く★★
```
  ★門★ ... ★台の 板と `sheets[i].data` が 同じか を 数える★
        ＝★式の 数／値の 数／食い違った マスの 数★を 出す
        ＝★食い違ったら 赤★
  ⇒★「1つ 忘れると 黙って 古い 値」を ★黙らせない★★
  ⇒★記憶「作る道が 2本 在る時は 両方 直せ」＝★道が 2本に なるので 門で 縛る★
```

---

## ④ ★★JS層と 台は どちらが 先か ── ★まだ 測って いません★★★

```
  ★経営者1 の 見立て★ ... JS層 -> 台 -> 借り物
  ★訳★ ... JS層には ★実測で 直した 物★が 在る
  ★★但し 2人とも 測って いません★★
  ⇒★★先に 測ります★★＝★JS層が 答える 式を 全部 出し、台と 突き合わせる★
  ⇒★違う 物が 在れば ★どちらが 実Excel と 合うか★で 決めます★
  ⇒★★これを 測らずに 順を 決めません★★（★今日 3回 踏んだ 形★）
```

---

## ⑤ ★★段取り（★1押し 1件★）★★

```
  ⑴★JS層と 台を 突き合わせる★（★測るだけ／画面は 触らない★）
  ⑵★板の 写しの 門を 書く★（★食い違ったら 赤★）
  ⑶★3段に する★（JS層 -> 台 -> 借り物）
  ⑷★経営者1 が 配信で 測る★（1,393 -> 1,466 か／下がる 0本 か）
  ⑸★下がったら 戻す★
```

★★⑴と ⑵を 先に やります★★＝★⑶は その 後★

---

## ⑥ ★言えない 事★

```
  ・★「+73」は ★紙の 上で 混ぜた 見込み★です★（経営者1 の 断り）
  ・★`convertFormula` を 通った 後の 式では 混ぜて いません★
  ・★JS層が 先に 横取りする 物を 数えて いません★
  ・★速さは 測って いません★（★板を 持つ／作り直す の どちらも★）
  ・★司さんの 実物（15,799式）では 1度も 動かして いません★
```
