# ★表示が 実Excel と どれだけ 違うか★（2026-09-25・監査役）

★司さんの 決め イ「★全く同じように表示できるようにしろや★」への 数★

---

## ★★★この 紙の 数は ★まだ 確定して いません★★★★

```
 ★私の 測り方に 3回 穴が 在りました★（★全部 Exally1 が 見つけました★）
   ⑴`cell.d` を 読んだ ............ ★描く 所を 見て いない★
   ⑵`_字の元` を 読んだ ........... ★書式を 掛ける 前の 値★
   ⑶`activeSheet` に 代入した ..... ★板を 「開いて」いない★
       （`switchSheet` は ★代入＋`loadSheetIntoEngine`＋`recalcSheet`★ の 3つ）
 ⇒★下の 数は ⑶を 直す 前の 物です★
 ⇒★9,163 → 116 と 減りましたが ★116 も 大きすぎる 可能性★が 在ります★
 ⇒★Exally1 が 名指しの マスを 開いたら ★実Excel と 同じに 出て いた★★
 ★確定して いる 物★
   ・★ゼロを 隠す 板が 15枚中 13枚★（実Excel に COM で 聞いた／`.xlsb` の バイトでも 一致）
   ・★答えは 1つも 悪く なって いない★（同じ 12,888 ／ 違う 2,911・★4回 とも 同じ★）
   ・★本物と 確かめた 違い ... 1個★（★居場所は 紙に 書きません＝板の 名が 入るので★）
       ★形だけ★ ... 実Excel `99999.9` ／ うち `99999.999` ／ 列の 幅 59 ／ 書式 `General`
       ＝★`General` の 桁の 決め方（`_入る字数`）が 違う★
       ⇒★居場所は 作り手に だけ 便りで 渡しました★（道具 117 の `--例 N`・★既定 0★）
 ★測り直しの 形★
   ⇒`switchSheet(i)` を ★1板ずつ★ 呼ぶ（★お客さんが 札を 押すのと 同じ★）
```

---

## ★★数の 動き（★どれも 測り方を 直す たびに 変わりました★）★★

| 画面の 字が 実Excel と 違う | いつ | 物差し／取り方 |
|---|---|---|
| 3,403個 | 朝 | ★借り物の `w`★（★曜日を 出せない／ゼロを 隠す★）⇒★捨てました★ |
| 9,163個 | 昼 | 実Excel ／ ★`cell.d` を 読んだ★ |
| 9,163個 | 昼 | 実Excel ／ `_字の元`（★ゼロ隠しの 直し前★） |
| 2,420個 | 夕 | 実Excel ／ `_字の元`（★書式を 掛けて いない★） |
| ★116個★ | 晩 | 実Excel ／ 書式まで 通した（★但し 板を 開いて いない★） |
| ★測り直し中★ | — | 実Excel ／ ★`switchSheet` で 板を 開く★ |

★★この 表そのものが この 紙の 一番 大事な 中身です★★
＝★数が 6回 変わり、★変わった 因は 5回 とも 私の 測り方★でした★。

---

## ★測った 材料と 物差し★

```
 材料 ... 代行計算表2026.xlsb 400,087 バイト
          sha256 2af78a40451f6f68fd08849f6a82b1b3fc72ac082cebb4cf67b08f9def28fabc
          置き場 C:\Users\zeroa\kyukyu-0907\（★OneDrive の 外の 写し★）
          板 15枚 ／ 字の 在る マス 21,227個 ／ ★式 15,799個★

 ★物差し★ ... ★実Excel が 出した 字（`Range.Text`）★
          道具 `toru-jitsu-excel-no-deta-ji.ps1`（120）
          ★読み取り専用で 開き `Close($false)`★／★1バイトも 書き戻しません★
          ★字は scratchpad にだけ★（★repo に 入れて いません＝お客さんの 数字★）
```

### ★★物差しを 1回 間違えました★★

```
 ★朝★ ... 借り物（SheetJS）の `w` を 物差しに した ⇒★3,403個 違う★
 ★但し 借り物は 曜日 `aaa` を 出せて いませんでした★
   ⇒`9/99(aaa) → 9/99(土)` が 87個＝★Exally の 方が 合って いた★
 ★もっと 悪い 事★
   ⇒★借り物の `w` も ゼロを 隠して いた★（★未測定・推測★）
   ⇒★6,743個が 見えて いませんでした★
 ★★だから 3,403 は 捨てました★★
 ⇒★実Excel を 物差しに した 9,163 が 本当の 数★
```

---

## ★★因ごとに 分けました（★これが 一番 効きました★）★★

### 朝（9,163個）

| 訳 | 数 | 因 |
|---|---|---|
| ★Excel は 空・うちは `0`★ | ★6,743個★ | ★「ゼロを 表示しない」★ |
| Excel は 空・うちは 0 でない 字 | 47個 | ★未測定★ |
| うちが 空・Excel は 字 | ★0個★ | — |
| 両方 字が 在るが 違う | 2,373個 | 書式 |

### 晩（2,420個）

| 訳 | 数 |
|---|---|
| ★Excel は 空・うちは `0`★ | ★0個★（★6,743 → 0★） |
| Excel は 空・うちは 0 でない 字 | 47個 |
| ★ゼロの 残り★ | ★9個★（下の 「残った 物」） |
| ★両方 字が 在るが 違う★ | ★2,373個★ |

### ★★2,373個の 内訳★★（★1マスは 1つだけ 数えます★）

| | 数 |
|---|---|
| ★㋐桁の 区切りが 無い★ | ★1,645個★ |
| ★㋒日付が 生の 数字★ | ★459個★ |
| ★㋑小数の 桁が 違う★ | ★224個★ |
| ㋓その他 | 45個 |
| ★合計★ | ★2,373個★（一致） |

★形（★数字は 9 に 伏せて います＝中身は 出しません★・多い順）★

```
   9,999   → 9999            505個   ㋐
   99,999  → 99999           340個   ㋐
   9,999   → 9999.9999999    264個   ㋐＋㋑（★丸めて いない★）
   9/99    → 99999           199個   ㋒
   99,999  → 99999.999999    125個   ㋐＋㋑
   9,999   → 9999.9          106個   ㋐＋㋑
   99,999  → 99999.9          96個   ㋐＋㋑
   9/9     → 99999            80個   ㋒
   99/99   → 99999            65個   ㋒
   99.99   → 99.9             61個   ㋑
   99,999  → 99999.99         54個   ㋐
   99.99   → 99               48個   ㋑
```

★見立て（★未測定★）★ ... ★㋐と㋑は 同じ 所かも しれません★（★書式を 掛けて いない／掛け方が 違う★）
⇒★1,645 ＋ 224 の 大半が 1つの 直しで 済む かも しれません★。★割るのは 作り手★。

---

## ★★6,743個の 因＝「ゼロを 表示しない」★★

```
 ★実Excel に 聞きました★（`$xl.ActiveWindow.DisplayZeros` を 板ごとに）
   板 1〜12 ... ★False（隠して います）★
   板 13・14 ... True
   板 15 ...... ★False★
 ⇒★15枚中 ★13枚★が ゼロを 隠して います★
 ⇒★Excel は 0 の マスを 何も 出さない／Exally は `0` を 出して いた★
 ⇒★計算の 間違いでは ありません＝板の 設定を 読んで いなかった★

 ★Exally1 が `.xlsb` の バイトでも 確かめました★（★別々の 道で 同じ 答え★）
   板の 記録 ★番号137 の 先頭バイト ビット4★
     `8c 03` ... 隠す（板 1〜7・9〜12・15）
     `cc 03` ... 隠す（板 8・★ビット6 は 別の 設定＝名前を 付けて いません★）
     ★`9c 03` ... 出す（板 13・14）★
   ⇒★COM で 読んだ True の 2枚と 1対1★
```

---

## ★★1回 「直したのに 1個も 動かない」が 出ました★★（★2つの 落ち★）

### ★落ち①（私）★ ── ★私が お客さんの 画面を 見て いなかった★

```
 Exally1 の 直しは ★描く 所★に 在り（`book.html:7179` `:7424`）★`cell.d` を 変えません★
 私は `cell.d` を 読んで いた ⇒★数が 動かないのは 当たり前★
 ⇒★直し★ ... ★お客さんの 道が 使って いる まさに その 関数を 呼ぶ★
     `window._字の元(cell)` ／ `window._ゼロを隠すか(cell, raw)`
     ★`_ゼロを隠すか` は `sheets[activeSheet]` を 見ます★（描くのは 1枚だから 正しい）
     ⇒★板ごとに `activeSheet` を 合わせます★
 ⇒★出しに 「どの 道で 字を 取ったか」を 必ず 書く★
     `客の道（_字の元＋_ゼロを隠すか）` ／ `d だけ（★古い 木★）`
```

### ★落ち②（作り手）★ ── ★式の マスでは 1つも 隠れて いなかった★

```
 ★直した 上で もう一度 測っても 1個も 動きませんでした★
 ⇒★「隠すと 判じた 数」を 道具に 出させました★
     判じた 式 13,544個 ／ ★隠すと 判じた ★0個★★
     中の 字が 0 の 式 ★6,752個★ ／ ★うち 隠さなかった 6,752個★
     ★`_字の元` が 返す 型 `{"string": 13544}`★（★13,544個 とも 字★）
 ★因（字を 追いました・★数が 先に 出て います★）★
     `js/book-open.js:542,547` ... 式の マスは ★`v` が 空の 字★／答えは ★`d`（String() で 作った 字）★
     `_字の元` ... `typeof cell.v === 'number'` が false ⇒ ★`cell.d`（字）を 返す★
     `_ゼロを隠すか` ... `typeof v === 'number' && v === 0` ⇒★いつも false★
 ⇒★★式の マスでは 1つも 隠れません★★
 ⇒★見本に 「式の 答えが 0 の マス」が 無かった★（`=D2-E2` の ような 物）
 ⇒★足したら 直す前に 戻すと 赤 1本★＝★門が 効く ように なりました★
```

> ★★覚え書き★★
> ★「9,163 が 動かない」だけでは 何も 分かりません★。
> ⇒★直した 所が ★何個に 効いたか★ を 道具に 出させる★。
> ⇒★0個なら その場で 分かります★。
> ★前は 自分の 物差しを 1周 疑って 遠回りしました★。

---

## ★★もう 1つ 踏みました＝1つの 取り方を 2つの 問いに 使った★★

```
 ゼロが 隠れる ように なった 途端
   ★「答えが 合って いるか」の 数が 12,888 → 6,145 に 落ちました★
 ⇒★答えが 壊れたのでは ありません★
 ⇒★私が 「画面に 出る 字」を 「答え」として 比べて いました★
   ＝隠れた 6,743個が 「答えが 空に なった」と 数えられた
 ⇒★2つ 別に 取ります★
     `生` ... ★答えそのもの★（隠すかどうかに 関わらない）⇒ 答えの 突き合わせ
     `画` ... ★画面に 出る 字★（隠したら 空）⇒ 表示の 突き合わせ
 ⇒★直した 後 ... 答えは 同じ 12,888／違う 2,911＝前と 1個も 同じ★
```

---

## ★★残った 物★★

| | 数 | 誰 |
|---|---|---|
| ★㋐桁の 区切りが 無い★ | ★1,645個★ | 作り手 |
| ★㋒日付が 生の 数字★ | ★459個★ | 作り手 |
| ★㋑小数の 桁が 違う★ | ★224個★ | 作り手 |
| ㋓その他 | 45個 | 作り手 |
| ★Excel は 空・うちは 0 でない 字★ | ★47個★ | ★未測定★ |
| ★ゼロの 残り★ | ★9個★ | 下 |

### ★9個について（★未測定＝見立てです★）★

```
 ★私の 数え方★ ... `Number(String(raw).trim()) === 0`
 ★作り手の 判じ★ ... `cell.f` が 在る ⇒ 答えの 字を 数に 読む
 ⇒★2つの 読み方が 9個で 食い違って います★
 ★考えられる 形★
   ・`d` が ★`0 円` の ような 書式つきの 字★
   ・`d` が ★`0.00`★／★`-0`★／全角の `０`
   ・★`cell.d字` が 立って いる★ ⇒★わざと 隠さない＝9個は 正しい＝直す 物は 無い★
 ⇒★「9個 とも `d字` が 立って いる」なら それで 終わりです★
```

---

## ★★「開いた 直後に 計算は 要るか」も 埋まりました★★

★司さん 09-25「必要ならいるし必要ないならいらんやろが／どんな状況でいるかいらんか言えや」★

### ★計算が いる 場合★

| | |
|---|---|
| ㋐ | ★開いた 日で 答えが 変わる 関数が 在る★（`TODAY` `NOW` `RAND` `OFFSET` `INDIRECT` `CELL` `INFO` ...） |
| ㋑ | ★式は 在るのに 答えが 本に 保存されて いない★ |
| ㋒ | ★Excel 以外の 道具が 作った 本★ |
| ㋓ | ★「開いたら 全部 計算しろ」の 印が 立って いる★ |
| ㋔ | ★お客さんが 打った 後★（★「開いた 直後」では ない＝削る 所では ない★） |

### ★いらん 場合★ ... ㋐〜㋓ が ★1つも 無い★

### ★★司さんの 実物★★

```
 ㋐ ... ★0個★（★名簿 9個を 探しました＝名簿に 無い 物は 見つかりません★）
 ㋑ ... ★0個★
 ㋓ ... ★立って いません★（★下の 「印の 在り処」★）
 答え ... ★数で 比べられた 12,888個は 全部 同じ★／★誤りを 返したのが 1個★
 ⇒★この 本で 開いた 直後に 計算する 理由は 1つも ありません★
 ⇒★但し 「この 本では」です★＝★他の 本では 立ちます★
 ⇒★形としては 「開いた 時に ㋐㋑㋓ を 数えて 当たる 本だけ 計算する」★
 ⇒★答えが 変わりうる 決め＝司さんの 決め★
```

### ★★`fullCalcOnLoad` の 在り処（★7冊を 1つずつ 作って 突き合わせました★）★★

```
 道具 `tsukuru-keisan-no-kime-1tsu-zutsu.ps1`（119）
   ★中身は 7冊 とも 同じ★（A1=1 A2=2 A3==SUM(A1:A2)）／★`.xlsb`（50）★
   ★`Application` の 決めは 1冊ごとに 元に 戻し、最後にも 戻して 数えました★
     Calculation=-4105 ／ Iteration=False ／ CalculateBeforeSave=True（★前後 同じ★）

 ★記録 157（26バイト）★
   0-moto        35 ea 02 00 ★01★ 00 00 00 64 00 00 00 fc a9 f1 d2 4d 62 50 3f 10 00 00 00 ★6a 00★
   1-tede        35 ea 02 00 ★00★ ...                                                        ★6a 00★
   2-kurikaeshi  35 ea 02 00   01  ...                                                        ★6e 00★
   3-hozonmae    35 ea 02 00 ★00★ ...                                                        ★4a 00★
   4-zenbu       35 ea 02 00   01  ...                                                        ★6a 01★
   5-1904        35 ea 02 00   01  ...                                                          6a 00
   6-seido       35 ea 02 00   01  ...                                                        ★62 00★
   ★実物★       ★34★ ea 02 00 01 ...                                        ★01★ 00 00 00 ★6a 00★

 ★読めた 事★
   ★5バイト目★ ... 計算の やり方（01 自動 ／ ★00 手で★）
   ★25バイト目（旗・もと `6a`）★
     ビット2 ... 繰り返し 計算（Iteration）        6a → ★6e★
     ビット3 ... 精度は 表示通り（★立つと 0★）    6a → ★62★
     ビット5 ... 保存前に 計算（★切ると 0★）      6a → ★4a★
   ★★26バイト目 ビット0 ＝ `ForceFullCalculation`★★   00 → ★01★
   ★1904年 起点は 157 では ない★ ... ★記録 153 の 1バイト目 ビット0★ 20 → ★21★
   ★21バイト目（実物 `01` ／ 私の 本 `10`）★ ... ★何か 分かりません＝名前を 付けません★

 ★2人が 別々の 道具で 読んで 1バイト残らず 同じ★
```

> ★★覚え書き＝この 答えは 1回 嘘を 出しました★★
> ★はじめ 私は 「`ForceFullCalculation` は ファイルに 残らない」と 出しました★。
> 因 ... 私の 道具 `yomu-xlsb-no-kiroku.mjs:185` が ★`slice(0, 24)`★ で 切って いた。
> ★記録 157 は 26バイト★ ⇒ ★変わる 26バイト目を 誰も 見て いなかった★。
> ⇒★決まった 字数で 切らない★／★切る 時は 「ここで 切りました（全 Nバイトの うち M）」と 出す★。

---

## ★道具★

| 番 | 道具 | 何を するか |
|---|---|---|
| 117 | `kazoeru-hiraita-ato-keisan-ga-iruka.mjs` | ㋐㋑㋓ を 数える ／ ★答えと 表示を 別々に★ 突き合わせる ／ ★因ごとに 分ける★ |
| 118 | `tsukuru-keisan-no-shirushi-2satsu.ps1` | 印だけ 違う 本を 2冊 |
| 119 | `tsukuru-keisan-no-kime-1tsu-zutsu.ps1` | ★計算の 決めを 1つずつ★ 変えた 本 7冊 |
| 120 | `toru-jitsu-excel-no-deta-ji.ps1` | ★実Excel が 出した 字★を 全マスぶん 取る（物差し） |

★117 の 門★
```
 ・★突き合わせ 0件は 赤（exit 6）★（★鍵の 形が 違うと 「全部 同じ」に 見えます★）
 ・★物差しを 出しに 必ず 書く★（実Excel か 借り物か）
 ・★どの 道で 字を 取ったか を 出しに 必ず 書く★（客の道か `d` だけか）
 ・★「隠すと 判じた 数」を 出す★
```
★120 の 門★
```
 ・★5.1／材料の 印／走らせる 前の Excel が 0個（★他の 席を 閉じません★）★
 ・★改行を バイトで 数え／書いた 数と 合わなければ 赤（exit 6）★
   （★`Get-Content | Measure-Object -Line` が 21,227 を 20,190 と 返しました＝嘘★）
```
★119 の 門★
```
 ・★`Application` の 決めを 1冊ごとに 戻し、最後に 型を 見て 数える★
   （★「空 と 空 は 同じ」で ✓ を 出した★ので ★型を 見るまで 緑に しません★）
 ・★戻すのは 本を 閉じる ★前★★（閉じると 触れません＝0x800A03EC）
```

★生の 出し★ ... `docs/measured/hyouji-ga-jitsu-excel-to-chigau.out.txt`

---

## ★★今日 踏んだ 型（★捨てません★）★★

```
 ⑴★突き合わせ 0件を 「全部 同じ」と 出した★（鍵の 形が 違った）
 ⑵★式の マスの `v` を 読んで 13,544個の 嘘の 違いを 出した★（答えは `d`）
 ⑶★CPU の 記録を 1回だけ 取って 出しかけた★（同じ 木で 33.0% → 1.1%）
 ⑷★道具が 24バイトで 切って いた★（記録 157 は 26バイト）
 ⑸★相手の 字を 数えずに 「あなたも 切って いる」と 言った★（★相手は 26バイト 出して いた★）
 ⑹★`= if (` を 因だと 言った★（★本当の 因は 括弧の 中の 行折り★）
     ＝★2つ 同時に 変えて どちらが 効いたか 見て いなかった★
     ★測った 決まり★ ... ★PowerShell は 括弧の 中でも 「行が 値で 終わる」と そこで 切る★
                       ⇒★続けるなら 行末を `+` に する★（★次の行を `+` で 始めては いけない★）
 ⑺★Git Bash から 数えて 「121本 全部 赤」と 出した★（★道の 作り方を 間違えた＝偽の 赤★）
     ⇒★PowerShell の 中で 回し直して 112本 緑／9本 赤★
     ⇒★因は 8本が BOM 無し★（★残り 1本は 別の 因・未測定★）
 ⑻★1つの 取り方を 2つの 問いに 使った★（答えと 表示）
 ⑼★heredoc で `\n` が 本物の 改行に なった★（★記憶に 在る 型・今日 2回★）
 ★8回とも 自分か 作り手が 数え直して 取り消しました★
 ★一番 効いた 直し★ ... ★道具に 「何個に 効いたか」を 出させる★
```
