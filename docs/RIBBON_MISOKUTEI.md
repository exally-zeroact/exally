# ★リボン：正本どうしの 食い違い（1行ずつ 読んだ）★ 2026-09-03

> ★★2026-09-03 訂正★★
> この紙の 前の版は「★未測定 12行★」と 書いていました。★間違いです★。
> ★12行とも 実測ファイルに 在りました★＝★組の 名前の 末尾に 空白★が 付いていただけ
> （`描画ツール` と `描画ツール␣`）。★私が 完全一致で 探して「無い」と 決めつけた★。
>
> ★同じ日に 同じ穴を 2回 踏みました★＝もう1つは「マクロ入りの xlsm が 手元に 0本」
> （★repo の `tests/fixtures/vba-sample.xlsm` に 在った★・司さんに 叱られて 気づいた）。
>
> ⇒ ★「未測定」と 書く前に、★字そのもの★で もう一度 探す★
> ⇒ ★「無い」と「見つけられなかった」は 別物★

---

## ★何を 突き合わせたか★

`docs/excel-ribbon-flat.tsv`（UIが 読む 正本）と
`docs/excel-ribbon-remeasure-2026-08-30.tsv`（★座標つきの 実測★）。

★タブ｜組｜品 が 完全一致しない 行 ＝ 28行★。★1行ずつ 中身を 読んだ 結果★：

| # | 何だったか | 行数 | どうしたか |
|---|---|---:|---|
| (1) | ★本物の 重複★（座標が 同じ・中身も 同じ） | ★4★ | ★消した★（下に 一覧） |
| (2) | 組の 名前の 違い（`Office スクリプト ギャラリー` ⇄ `Office スクリプト`） | 8 | ★触らない★（同じ物） |
| (3) | ★組の 名前の 末尾に 空白★（`描画ツール` ⇄ `描画ツール␣`） | ★12★ | ★触らない（同じ物）★ |
| (4) | 品の 名前の 末尾に 空白（`アドイン␣` `データ型アイコン␣`） | 2 | ★触らない★（同じ物） |
| (5) | 組が 空欄で 測れている（`データ｜｜地理 (English)` 左1006 上295） | 1 | ★触らない★（同じ物） |
| (6) | ★本当に 実測ファイルに 無い★ | ★1★ | ★下に 書く★ |

★＝「測れていない」のは ★1行だけ★★（前は 12行と 書いていた）。

---

## ★(6) 本当に 実測ファイルに 無い … 1行★

```
 ページ レイアウト｜拡大縮小印刷｜拡大/縮小:
```
★実測ファイルの その組★＝`横:`(ComboBox+Edit) / `縦:`(ComboBox+Edit) / `増やす` / `減らす` /
`開く`×2 / `ページ設定`（上=321＝↘）。★`拡大/縮小:` だけ 入っていない★。

★どちらかは 決められません（決めつけない）★
- ①実Excel に 在るのに ★測り落とした★（横:・縦: と 同じ 組の 3つ目の 入力）
- ②実Excel に 無い

★確かめ方★＝実Excel の ページ レイアウトを 開いて ★その組の 中の 入力を 数える★
（`tools/measure-*.ps1` を `pwsh` で。★`powershell`(5.1) は 日本語が 化ける★）

---

## ★消した 行（2026-09-03・全部 控えつき）★

### ★(1) 同じ場所・同じ中身の 重複 … 4行★（シートのオプション）
```
 ページ レイアウト	シートのオプション	枠線 / 表示 / 印刷 / 見出し
```
★座標で 確定★
```
 枠線   表示/印刷 CheckBox 左1335（上216/264）
 見出し 表示/印刷 CheckBox 左1464（上216/264）
 シートのオプション | シートのオプション | ★Button 左1551 上321★  ←★この組の 中身は これ1つ★
 配置 の 先頭は 左1599  ⇒ 1335〜1551 が ★ひと箱★
```
⇒ ★同じ 4つを 2度 持っていた★＝画面に「表示」が4つ・「印刷」が3つ 並んでいた。
⇒ 今は ★1つの 箱の 中に 枠線[表示][印刷]／見出し[表示][印刷] の 2列★（`s:` ＝親の字）。

### ★(1') 同じ場所・同じ働きの 分かれボタン … 4行★
```
 数式	定義された名前	名前の定義...      （名前の定義 と 同じ字・同じ働き）
 ホーム	編集	合計                    （オート SUM と 同じ働き）
 数式	関数ライブラリ	合計            （同上）
 表示	マクロ	マクロの表示            （マクロ と 同じ働き）
```
★UIA は 分かれボタンを ★同じ座標で 2回★ 返す（実測 9組）★。
★そのうち 働きが 同じ 4行だけ 消した★。

### ★残した 5行（同じ場所・★働きが 違う★）★
```
 ホーム｜セル｜挿入(セルを挿入) と セルの挿入(行を挿入)
 ホーム｜セル｜削除(行を削除)   と セルの削除...(セルを削除)
 ホーム｜フォント｜罫線(窓)     と 下罫線(すぐ 下罫線)
 ホーム｜フォント｜塗りつぶしの色(窓) と 〃RGB(255,255,0)(すぐ 黄)
 ホーム｜フォント｜フォントの色(窓)   と 〃RGB(255,0,0)(すぐ 赤)
```
★消すと「1押しで 出来ていた事」が 2段に 落ちる★（司さん 2026-08-31）ので ★残す★。

★控え★ … `scratchpad/flat.before.tsv`(318行) / `flat.before4.tsv` / `flat.before5.tsv`
★今★ … 310行

---

## ★逆向き＝実測に 在って flat.tsv に 無い 行 … 71行★

★こちらは まだ 1行ずつ 読んでいません（★未着手★＝「無い」とは 言いません）★。
タブ別の 数（2026-09-03 実測）：
```
 描画15 ／ Sheet1 13 ／ 表示5 ／ ホーム5 ／ データ5 ／ 開発4 ／ 自動化4 ／
 校閲4 ／ 数式4 ／ 挿入4 ／ ヘルプ4 ／ ページ レイアウト4 …
```
※`Sheet1` の 13行は ★下の シート見出しの 写り込み★＝リボンでは ない
（`lib/ribbon-scope.js` の 出さない[] に 理由が 書いてある）。
★次に やるなら ここ★＝★71行を 1行ずつ 読む★（上の 28行と 同じ やり方で）。
