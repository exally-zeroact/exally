# ★リボン：まだ 測れていない物（消さずに 積む）★ 2026-09-03

> ★「無い」と「未測定」を 分ける★。
> ★測っていない物を 消すと 記録が 嘘に なる★ ので、ここに 積んで 後で 測る。

---

## ★なぜ この紙が 要るか★

2026-09-03 に `docs/excel-ribbon-flat.tsv`（UIが 読む 正本）と
`docs/excel-ribbon-remeasure-2026-08-30.tsv`（★座標つきの 実測★）を 突き合わせたら、
★flat.tsv に 在って 実測に 無い 行が 28行★ 出た。

★中身を 1行ずつ 読んだら 5種類だった★（★「実物に 無い」は そのうち 4行だけ★）。

| 種類 | 行数 | どうしたか |
|---|---:|---|
| ★(1) 本物の 重複★ | ★4★ | ★消した★（下に 一覧） |
| (2) 組の 名前の 違い | 8 | ★触らない★（同じ物） |
| ★(3) 実測ファイルに その組が 1行も 無い★ | ★12★ | ★触らない＝この紙に 積む★ |
| (4) 末尾の 空白の 違い | 2 | ★触らない★（同じ物） |
| (5) 要 確認 | 2 | ★触らない＝この紙に 積む★ |

---

## ★(3) 未測定 … 描画｜描画ツール 12行★

★実測ファイルに `描画|描画ツール` の 行が 1行も 無い★。
＝★「実Excel に 無い」ではなく「測っていない」★。

```
 なげなわ選択: インクを選択して図形を描画します。図形内のすべてのインクストロークが選択されます。
 アクション ペン: アクション ペンを使用して、手書きでデータを入力します。
 ブラシ ペン: 緑、1 mm
 ペン: 赤、0.35 mm
 ペン: 黒、0.35 mm
 万年筆: 濃い青、1 mm
 描画ツール
 消しゴム: ページに描画したインクを消しゴムで消します。
 蛍光ペン: 黄, 6mm、テキストに位置を合わせる: オフ
 追加
 選択: インク、図形、テキスト領域などのオブジェクトを選びます。これは特に、テキストの背後にあるオブジェクトを操作するときに使うと便利です。
 鉛筆書き: 灰色、0.5 mm
```

★測り方★＝実Excel の `描画` タブを 選んでから UI Automation で 取る
（★物を 選ばないと 出ない 部品が 在る★＝2026-08-30 に 学んだ事）。

---

## ★(5) 要 確認 … 2行★

| 行 | 何が 分からないか |
|---|---|
| `データ｜データの種類｜地理 (English)` | 実測は ★株式 (English) と 通貨 (English) の 2つだけ★。`地理` が 取れていない。★取りこぼしか、実物に 無いか 未確定★ |
| `ページ レイアウト｜拡大縮小印刷｜拡大/縮小:` | 実測は 横:／縦:／増やす／減らす／ページ設定／開く。★`拡大/縮小:` が 取れていない★ |

---

## ★逆向き＝実測に 在って flat.tsv に 無い 行 … 71行★

★こちらは「うちの 正本が 足りない」側★。タブ別の 数（2026-09-03 実測）：

```
 描画15 ／ Sheet1 13 ／ 表示5 ／ ホーム5 ／ データ5 ／ 開発4 ／ 自動化4 ／
 校閲4 ／ 数式4 ／ 挿入4 ／ ヘルプ4 ／ ページ レイアウト4 …
```
※`Sheet1` の 13行は ★下の シート見出しの 写り込み★＝リボンでは ない
（`lib/ribbon-scope.js` の 出さない[] に 理由が 書いてある）。

---

## ★消した 4行（2026-09-03・控えを 取ってから 消した）★

```
 ページ レイアウト	シートのオプション	枠線
 ページ レイアウト	シートのオプション	表示
 ページ レイアウト	シートのオプション	印刷
 ページ レイアウト	シートのオプション	見出し
```
★消してよいと 言い切れた 理由（座標）★
```
 枠線   表示 CheckBox 左1335 上216 ／ 枠線   印刷 CheckBox 左1335 上264
 見出し 表示 CheckBox 左1464 上216 ／ 見出し 印刷 CheckBox 左1464 上264
 シートのオプション | シートのオプション | ★Button 左1551 上321★  ←★この組の 中身は これ1つ★
 配置 の 先頭は 左1599
 ⇒ 1335〜1551 が ★ひと箱★＝箱の 名前が「シートのオプション」
 ⇒ ★同じ 4つを 2度 持っていた＝画面に 「表示」が 4つ・「印刷」が 3つ 並んでいた★
```
★消す前の 控え★ … `scratchpad/flat.before.tsv`（318行）／消した後 314行
