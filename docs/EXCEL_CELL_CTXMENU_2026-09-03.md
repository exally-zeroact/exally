# ★実Excel の セルの 右クリック＝これが 正本★ 2026-09-03

> ★67 は もう 使いません★（＝`CommandBars("Cell")` の 一覧＝★実際には 出ない物まで 入っていた★）。
> ★お客さんが 実際に 見るのは 20行★。それを ここに 置きます。
> ★08-30 の `docs/excel-commandbars-2026-08-30.tsv` は 消しません★＝「★在る物の 一覧★」としては 正しい。

---

## ★測り方（次の人が 4通り 試さずに 済むように）★

```
 環境 … Excel 16.0 build 20326 ／ 新規ブック ／ 読むだけ ／ 保存しない ／ 終わったら 閉じた
 道具 … tools/cell-ctxmenu-dump.ps1（+ 絵を 撮る）

 ★踏んだ 罠と 直し（全部 実測）★
  ①★画面が スクリーンセーバーだった★
     GetForegroundWindow()=0／キーも マウスも 届かない／メニューも 出ない
     ⇒ ★OpenInputDesktop の 名前を 見る★（Default／Screen-saver／Winlogon）
     ⇒ ★「セッションの 制限」と 決めつけない★（私は 一度 そう 書いて 間違えた）
  ②★Windows は 後ろの プロセスからの 前面化を 断る★
     SetForegroundWindow だけでは Excel が 前に 来ない
     ⇒ ★AttachThreadInput で 今 前面の スレッドに くっついてから 頼む★と 通る
  ③★別の 窓を 押していた★（1回目は 後ろの Chrome の メニューが 開いた）
     ⇒ ★押す前に AutomationElement.FromPoint で「その 点に 居るのは Excel か」を 確かめる★
  ④★画面の 拡大率★ … SetProcessDPIAware を 呼ばないと 別の 場所を 押す
  ⑤★入力が 届いているか 先に 確かめる★
     A1 に「1」+Enter を 打って ★A1 = 1★ を 見てから 本番の 右クリックへ
  ⑥★UI Automation では メニューが 1個も 見えない★
     Menu 0 ／ 新しい トップレベル窓 0 ／ RawView も XLMAIN だけ
     ⇒ ★絵を 撮って 目で 読む★（★「UIAで 見えない」＝「無い」では ない★）
  ★ShowPopup() は 使わない★（2026-09-02 に Excel が 2分 固まった）
  ★CommandBars の .Visible も 使わない★（出るのに false が 返る）

 絵 … scratchpad/shot/excel_cell_ctxmenu_jitsubutsu.png
```

---

## ★★実物 20行（上から 並び順のまま）★★

| # | 実Excel の 字 | 印 | うちに 在るか |
|---|---|---|---|
| 1 | 切り取り(T) | | ★在る★ |
| 2 | コピー(C) | | ★在る★ |
| 3 | 貼り付けのオプション: | ★アイコン2つの 束★ | ★在る（貼り付け／値のみ貼り付け）★ |
| 4 | 形式を選択して貼り付け(S)... | 窓 | ★無い★ |
| 5 | 挿入(I)... | ★窓＝聞く★ | 在るが ★聞かない★ |
| 6 | 削除(D)... | ★窓＝聞く★ | 在るが ★聞かない★ |
| 7 | 数式と値のクリア(N) | | ★在る★ |
| 8 | クイック分析(Q) | ★灰★ | ★無い★ |
| 9 | フィルター(E) | ▸ | ★在る★ |
| 10 | 並べ替え(O) | ▸ | ★在る★ |
| 11 | テーブルまたは範囲からデータを取得(G)... | 窓 | ★無い★ |
| 12 | 新しいコメント(M) | | ★名前が 違う（コメントの挿入）★ |
| 13 | 新しいメモ(N) | | ★無い★ |
| 14 | セルの書式設定(F)... | 窓 | ★在る★ |
| 15 | ドロップダウン リストから選択(K)... | | ★無い★ |
| 16 | ふりがなの表示(S) | | ★在る★ |
| 17 | 名前の定義(A)... | 窓 | ★在る★ |
| 18 | リンク(I) | ▸ | ★在る（ハイパーリンク）★ |
| 19 | ハイパーリンクを開く(O) | ★灰★ | ★無い★ |
| 20 | この範囲にリンクする(L) | | ★無い★ |

★数え方（決めた事・ここに 書いておく）★
```
 ・★分母 ＝ 20★（★灰の 2つ（クイック分析／ハイパーリンクを開く）も 分母に 入れる★
   ＝「今 押せない」だけで ★お客さんの 目には 見えている★ から）
 ・★「貼り付けのオプション:」は 1行と 数える★（中の アイコンは 2つ）
   理由＝★お客さんが 見るのは 1行★。中身の 数は この 紙に 書いておけば 足りる。
```

## ★★出なかった物（大事）★★

```
 ★印刷範囲 ／ 改ページ ／ ページ設定 …… 1つも 出ません★
 ★コメントの削除 ／ コメントの表示/非表示 … 出ません★
   （コメントが 無い セルだから。★コメントが 在る セルでは 別に 測り直す★＝未測定）
 ★行の高さ ／ 列の幅 ／ 非表示 ／ 再表示 …… 出ません★
   ＝★これらは 実Excel では ★行／列の 帯★の メニューに 在る★
```

## ★3つに 分けて 数える（1本の 数字を 使わない）★

```
 ★2026-09-03 の 実測（node scripts/count-ctx-menu.mjs）★

 ①★実Excel の セルの 右クリックに 在る 物 … ★13 / 20★★  ←★本命★
    足りない 7 … 形式を選択して貼り付け【窓】／クイック分析【灰】／
                  テーブルまたは範囲からデータを取得【窓】／★新しいメモ★／
                  ドロップダウン リストから選択／ハイパーリンクを開く【灰】／この範囲にリンクする
 ②★行／列の 帯から 借りてきた 物 … 13個★（★消さない★）
    挿入・列の挿入・行の削除・列の削除・行の高さ・列の幅・
    非表示・列の非表示・再表示・列の再表示・印刷範囲・改ページ・ページ設定
 ③★うち独自の 物 … 7個★
    ウィンドウ枠の固定・固定の解除・印刷・条件付き書式・入力の決まり・
    決まりに合わない値・AIに解説させる
 ④★実Excel でも ▸ の 中／条件つきで 出る 物 … 9個（★中身は 未測定★）★
    選択したセルの値でフィルター・フィルターのクリア・再適用・
    昇順・降順・ユーザー設定の並べ替え・ハイパーリンクの削除・
    コメントの削除・コメントの表示/非表示
    ⇒★▸ は マウスを 乗せないと 開かない／コメントの2つは コメントが 在る セルで 測り直す★

 （うちの 札を 子まで 歩いた 数 ＝ 43 ＝ 14＋13＋7＋9。
   ★14 が 13行に なるのは「貼り付け」と「値のみ貼り付け」が
     実Excel では 1行（貼り付けのオプション:）に なっている 為★）

 ★数は 手で 書かない★ ⇒ `node scripts/count-ctx-menu.mjs`
 ★見張り★ tests/ctx-menu-items.test.mjs（★13 を 下回ったら 赤★）
```

## ★★▸ の 中も 測りました（2026-09-03・実物）★★

★開き方＝アクセスキー★（お客さんの 道）。絵 … `shot/excel_sub_filter.png` `_sort.png` `_link.png`

```
 ★フィルター(E) ▸ ＝ 6個★（うち ★3 / 6★）
   ★フィルターのクリア(E)【灰】★／★再適用(R)【灰】★／選択したセルの値でフィルター(V)／
   ★選択したセルの色でフィルター(C)★／★選択したセルのフォント色でフィルター(E)★／
   ★選択したセルのアイコンでフィルター(I)★        ←★後ろ3つが うちに 無い★

 ★並べ替え(O) ▸ ＝ 6個★（うち ★3 / 6★）
   昇順(S)／降順(O)／★選択したセルの色を上に表示(C)★／
   ★選択したフォントの色を上に表示(F)★／★選択した書式設定のアイコンを上に配置(F)★／
   ユーザー設定の並べ替え(U)...                    ←★色の3つが うちに 無い★

 ★リンク(I) ▸ ＝ 2個★（うち ★0 / 2★）
   「最近表示したアイテム」（今は ★利用できる項目がありません★）／リンクを挿入(I)...
   ⇒★うちの「ハイパーリンク」は 親の 行に 当てている★（子の 形は 真似ていない）
```

★★ここで 1つ 罠に 当たりました★★
```
 ★「リンク(I)」と「挿入(I)」は ★同じ アクセスキー I★★
 ⇒ I を 1回 押すと ★挿入の方★が 選ばれる（絵で 分かった）
 ⇒★2回 押して リンクへ 移り、→ で ▸ を 開く★
 ★もう1つ★＝Esc を 1回しか 押さずに 次の 右クリックへ 行くと
   ★親メニューが 開いたまま＝右クリックが「閉じる」だけに なり、
     次に 押した 文字が ★セルに 入って しまう★★（実測）
   ⇒★Esc を 3回＋選び直し★を 入れた
```

## ★まだ 測っていない（条件が 要る）★

```
 ★コメントの削除 ／ コメントの表示/非表示★ … ★コメントが 在る セル★で 測り直す
 ★ハイパーリンクの削除★ …………………… ★リンクが 在る セル★で 測り直す
   （リンク(I)▸ の 中には ありませんでした）
```

## ★★A で 足した 分（2026-09-03・実測）★★

```
 ①★ハイパーリンクを開く★ … ★リンクが 在る時だけ 押せる／無ければ 灰★（実Excel と 同じ）
    絵 … shot/a01_link_nashi_hai.png（灰 #9AA8A0）／a02_link_ari.png（黒＝押せる）
 ②★ドロップダウン リストから選択★ … ★一覧が 長い時だけ 1行に する★
    ★個数では 決めない★＝★「切り取り」が 転がさずに 見えるか★で 決める
      1280x720 … 19個まで 一覧のまま ／ 20個から 1行
      1280x560 … ★14個まで 一覧のまま ／ 15個から 1行★
      ⇒★同じ 中身でも 窓の 高さで 境目が 動く★＝スマホでも 壊れない
    絵 … shot/list50.png（★直す前＝一覧だけで 切り取りが 消える★）
         shot/a03_list50_ato.png（★直した後＝1行＋切り取りが 見える★）
    ★決まり 4つ★
      ・測るのは 出す時に 1回だけ（二度 走らない）
      ・★測れない時は 安全側＝1行★（高さ0・まだ 描かれていない）
      ・短い時は 今のまま（★1押しで 出来ていた物を 2段に 落とさない★）
      ・見張りは ★個数では なく「切り取りが 見えるか」★
 ⇒★①13 / 20 → ★15 / 20★★（作らない 1／足りない 4）
 ★見張り★ tests/ctx-menu-a-ui.test.mjs（jsdom で 本物の 画面を 載せる・★13緑★）
   ・灰／黒の 出し分け／★灰を 押しても 閉じない★
   ・★測れない時は 1行★（「一覧のまま」なら 赤）
   ・★短い時は 一覧のまま／長い時は 1行★＝★作り物の 高さを 与えて 見る★
       箱 704px … 19個は 一覧／★20個で 1行★  ←★実ブラウザの 720px と 同じ 数★
       箱 400px … 10個は 一覧／★11個で 1行★
     ＋★箱が 低いほど 早く 1行に なる★（個数 固定では ない事を 数で 見る）
   ・★個数で 決めていない★（`items.length <= 数` なら 赤）／わざと 壊して 3通り 赤

 ★★本物の パソコンで 押した（2026-09-03・司さん「パソコンで確認しろ」）★★
   ★本物の ブラウザ・★本物の マウス★★（Playwright の click{button:'right'} / click()）
   ★JSで イベントは 投げていない★／★先に 配信の バイトが 枝 009bc82 と 一致★を 確かめた
     book.html 94d3d5b83adac953 ／ lib/ctx-menu.js 8143aaecb4e23122
   ★Vercel の 下見URL は ログインが 要って 開けない★（★パスワードは 打たない★）
     ⇒ 同じ バイトを 手元の 配信で 開いた（★何で 見たかを 隠さない★）
   ┌ 1280x720 ／ 一覧 50個
   │  ★一番上＝「☑ ドロップダウン リストから選択」1行★／その下に 切り取り・コピー…が 見える
   │  ハイパーリンクを開く＝★灰★／★1行を 押す → その場で 50個の 一覧★（メニューは 開いたまま）
   │  絵 … shot/pc720_50ko.png
   └ 1280x560（低い 画面）
      ★14個＝一覧のまま★（切り取り 496px／箱 544px）
      ★15個＝1行★（切り取り 48px／箱 544px）
      リンク在りの セル … ハイパーリンクを開くが ★黒 rgb(26,43,34)＝押せる★
      絵 … shot/pc560_15ko_link.png

 ★★言い方の 決まり（2026-09-03）★★
   ★「未測定」と「機械が 毎回は 見ていない」は 別物★
     ・★未測定★ ………………… ★一度も 見ていない★＝★数字を 出しては いけない★
     ・★機械が 毎回は 見ていない★ … ★見た事は 在る／自動で 見張っていないだけ★
   ⇒ ここは ★後者★。★CI に 実ブラウザは 在りません★（workflows 4本・playwright の字 0件）
   ⇒★週1の 実ブラウザの 回（B の 後）で 埋める★（正本は rakually-test の webkit.yml を 借りる）
```

## ★★見張りの 穴（2026-09-03 に 直した・大きい）★★

```
 `tests/ctx-menu-items.test.mjs` が 画面の 関数を 探す 形
   前 … `function\s+([A-Za-z_$][\w$]*)\s*\(`  ⇒★英語の 名前しか 拾えない★
 ★book.html の 関数は ほとんど 日本語★（リンクを開く・名前の窓を開く …）
 ⇒★生きている 関数を「死んでいる」と 誤って 赤に した★（実際に 踏んだ）
 ⇒ 直したら 見ていた 関数が ★289個 → 805個★（★64%が 見えていなかった★）
 ★同じ 型★＝`` が 日本語の 後で 効かない（08-30）／
   ★日本語を 英語の 形で 探して 0件★＝「0件は 探し方が 甘いだけ」
```

## ★棚（今回は 直さない）★

```
 ①★「コメントの挿入」は 実物と 名前も 数も 違う★
    実物＝★新しいコメント＋新しいメモ の 2つ★／うち＝コメントの挿入 1つ
 ②★挿入／削除は 実Excel が 詰める向きを 聞く（...）★／うちは 聞かずに 上へ・下へ
 ③★「挿入」「非表示」「再表示」に 行の 字が 無い★
    ＝★行/列の 帯を 1枚に まとめている★のが 元の 理由
 ⇒★①②③は 1つの 束★＝★バラバラに 直すと また ちぐはぐに なる★
```
