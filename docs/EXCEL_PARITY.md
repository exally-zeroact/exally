# Excel との差 — 端から全部 数えた表

作成 2026-08-18 ／ 方針の上書き（司さん）**「Excelの最上級として作る／つけられる機能は全て作る」**を受けて。
**★この表は「作る前に数える」ための物。毎回この数字を更新して報告する★**

---

## 0. ★数え方（どこまで機械で数えたか・正直に）★

| 何 | どうやって数えたか | 機械か |
|---|---|---|
| **うちのショートカット** | 本物の `book.html` を載せて **1つずつ実際に keydown を送り**、画面の状態が変わったか／既定の動きを止めたかを見た | **★機械★** |
| **うちの操作（ボタン等）** | 本物の `book.html` の DOM から `button / select / input / onclick` を全部 拾った | **★機械★** |
| **Excelの右クリック** | **実Excel(COM)** の `CommandBars` から項目名をそのまま取った | **★機械★** |
| **Excelのリボン** | **実Excel を UI Automation で** 読み、タブ→グループ→部品を **並び順のまま** 取った（`tools/ribbon-dump.ps1`）。正本＝`docs/excel-ribbon-flat.tsv` | **★機械★** |

**★2026-08-29 に 差し替えた★**。ここには 長い間 **「リボンだけは人が書いた（COMからは取れない）」** と 書いてあった。
**★「COMで取れない」＝「機械で取れない」ではなかった★**。UI Automation で 並び順のまま 取れる。
（経営者セッションが 30分で 道具を作って 測った。**★測る道具が無いなら 作る★**）

数えた実Excel = **16.0.20228**（日本語UI 1041・`tools/golden-excel.ps1` と同じ1版）
／リボンを測った版 = **16.0 build 20326**（日本語UI）。

---

## 1. ショートカット（★実際に押して数えた★）

**★42個を数えて 在る 40 ／ 無い 2★**（2026-08-21 実測。この日の朝は 37／5、08-18 は 36／6、その前は 21／21）

> 数え方は前と同じ＝**1つずつ実際に keydown を送って、既定の動きを止めたか／画面の状態が変わったか**を見る。
> ★F4 は この数え方では「無い」に入る★＝式を編集していない時は何もしないから。
> **F4 の $ の切替は 編集中だけ効く**（`node tests/excel-shortcuts.test.mjs` が別に押して確かめている）。

> ★指示役の「Ctrl系のショートカットが実質ゼロ」は誤り★。
> Ctrl+C/V/X/Z/Y/A・Ctrl+矢印・Ctrl+Shift+矢印・Ctrl+Home/End は **前から在った**。

| キー | 何をする | 今 | 実Excelを測ったか |
|---|---|---|---|
| Ctrl+C / V / X | コピー／貼り付け／切り取り | **在る** | 前から |
| Ctrl+Z / Y | 元に戻す／やり直す | **在る** | 前から |
| Ctrl+A | 全選択 | **在る** | 前から |
| Ctrl+↑↓←→ | 端まで移動 | **在る** | 前から |
| Ctrl+Shift+↓→ | 端まで選択 | **在る** | 前から |
| Ctrl+Home / End | A1へ／最後のセルへ | **在る** | 前から |
| F2 / Home / End / Delete / Enter / Tab / 矢印 | 編集・移動・消す | **在る** | 前から |
| **Ctrl+S** | 書き出す | **★作った★** | 測る物なし |
| **Ctrl+B / I / U** | 太字／斜体／下線 | **★作った★** | 測る物なし（ボタンと同じ） |
| **Ctrl+1** | セルの書式設定 | **★作った★** | 測る物なし（ボタンと同じ） |
| **Ctrl+D / R** | 上／左のセルをコピー | **★作った★** | ★測った★ 式も書式も写す・参照はずれる |
| **Ctrl+;** / **Ctrl+:** | 今日の日付／今の時刻 | **★作った★** | ★測った★ 書式 `yyyy/m/d` ／ `h:mm` |
| **Shift+Space / Ctrl+Space** | 行を選ぶ／列を選ぶ | **★作った★** | 測る物なし |
| **Ctrl+PageUp / PageDown** | シート移動 | **★作った★** | 測る物なし |
| **PageUp / PageDown** | 1画面 上下 | **★作った★** | 測る物なし |
| **F4** | 直前の操作を繰り返す／$の切替 | **一部**（★$の切替だけ★） | 4つの形は★測った★／**回る順番と「繰り返す」は未測定** |
| **Ctrl+F / Ctrl+H** | 検索／置換 | **★作った★**（2026-08-21） | ★測った★ 置換は式を見る／* ? ~／大文字小文字は既定で区別しない |
| **Alt+=** | オートSUM | **★作った★**（2026-08-21） | ★測った★ 指示役が実Excelを押して3通り（下） |
| **Esc** | やめる（コピーの点線を消す） | **無い** | うちはコピーの点線を描いていない |
| **Ctrl+P** | 印刷 | **★作った★**（2026-08-21） | ★測った★ A4縦・余白・枠線なし（★紙だけの新しい窓で刷る★） |

### ★測っていない物は作っていない（理由つき）★

| 何 | なぜ作らなかったか |
|---|---|
| **Alt+= の「上と左の両方が すぐ隣に在る時」** | 測った3通りでは その形にならない。**上を先に見る**で作ってある（3通りと矛盾しない）＝★実物で1回 押して確かめてもらう物★ |
| **Alt+= の「入れた後の待ち方」** | Excel は式を入れて **編集中のまま待つ**（Enterで確定）。うちは **そのまま入れる**。ここは測っていない＝違いとして記録 |
| **F4 の「回る順番」** | $ の4つの形（`$A$1` / `A$1` / `$A1` / `A1`）は `ConvertFormula` で測った。**順番だけは押せないので未測定**。Excel の説明どおりの順で作ってある＝★司さんに一度 実物で押して確かめてもらう物★ |
| **F4 の「直前の操作を繰り返す」** | 「どこまでを1操作と数えるか」を測れていない |
| **Esc（編集していない時）** | Excel は「コピーの点線を消す」。うちは点線を描いていないので、作っても見た目が何も変わらない＝入れていない |

### ★Alt+= が当てる範囲（2026-08-21 指示役が実Excelを押して測った）★

| 置いた物 | 押した所 | 出た式 | 意味 |
|---|---|---|---|
| A1〜A10 に数10個 | A11 | `=SUM(A1:A10)` | 上へ たどる |
| C1〜C3 数・**C4 空**・C5 数 | C6 | `=SUM(C5)` | **★空きで止まる（離れた物は拾わない）★** |
| F1〜F3 縦・C8〜E8 横 | F8 | `=SUM(C8:E8)` | **★すぐ上(F7)が空なので 横が採られる★** |

作りは `lib/autosum.js`（純関数）。見つからなければ **何も入れず「足す数が見つかりません」と出す**。

**真値の置き場**: `tests/fixtures/excel-shortcuts-golden.json`（実Excel 16.0.20228 から取った）
**確かめ方**: `node tests/excel-shortcuts.test.mjs` … ★本物の book.html に実際に keydown を送って 26項目★

---

## 2. うちの画面に今 在る操作（★機械で数えた★）

**★332個（同じ物を除くと 327個）★**（2026-08-30。★リンクの窓＋Ctrl+Aの直しで 増えた★／2026-08-29 は 326（321）。★指示役の裁定＝B案★＝上は 名前とファイル名だけ／操作は 下の帯5つ（ホーム・読み込む・書き出す・その他・設定）／数式帳は「その他」の中／その前は 231（226））

> ★数え方★：`node tests/excel-parity.test.mjs`。
> 　本物の book.html を載せて `button / select / input / [onclick]` を 全部 拾う（同じ字＋同じonclick は1つに畳む）。
> 　★この数は 検査が毎回 数え直していて、古くなると 赤くなります★（手で書き換える数ではありません）。

内訳の主な物:

- 文字 … 太字・斜体・下線・取り消し線・サイズ・文字色・背景色
- 揃え … 左／中央／右・折り返し・任意改行・インデント増減
- 数の形 … ％・桁区切り・小数点増減・¥・セルの書式設定（表示形式/フォント/罫線/配置/塗りつぶし）
- 罫線 … 全／外枠／内側／なし ・ セル結合
- 編集 … 元に戻す／やり直す・関数の挿入(fx)・数式バー・名前ボックス
- 右クリック … 切り取り／コピー／貼り付け／値のみ貼り付け／削除／書式設定／
  行列の挿入・削除・非表示・再表示／**★昇順で並べ替え・降順で並べ替え・選んだセルの値で絞り込む・絞り込みを解除・ここで固定・固定をやめる（2026-08-21）★**／**★条件付き書式（2026-08-22）★**
- ファイル … **Excelを読み込む／Excelに書き出す**（+ 書き出す前の差分プレビュー）
- その他 … シートタブ・シート追加・複数選択・Excel版の切替・AIに話しかける

---

## 3. Excelの右クリック（★実Excelから機械で取った★）

| どこ | Excelの項目数 | うち |
|---|---|---|
| セル | **51** | 11（切り取り/コピー/貼り付け/値のみ/削除/書式設定/行列の挿入・削除・非表示・再表示） |
| 行 | **23** | 4 |
| 列 | **23** | 4 |
| シートタブ(Ply) | **13** | 0（タブを右クリックしても何も出ない） |
| ブックのタブ | **16** | 0 |

**★セルの51項目のうち、うちに在るのは11★**（Copilot・翻訳・Python など Microsoft 固有の物を含む数）。
Microsoft 固有の物を除いた「表計算として要る物」は別途 数え直す（次の更新で）。

---

## 4. リボン（★実Excelから 機械で 取った・並び順のまま★）

> **正本 = `docs/excel-ribbon-flat.tsv`**（タブ＼tグループ＼t部品 の3列）／測る道具 = `tools/ribbon-dump.ps1`
> **★版が変わったら 道具を もう一度 走らせて 数え直す★**

### ★実測（2026-08-29・実Excel 16.0 build 20326 日本語UI）★

**タブ 12 ／ グループ 67 ／ 部品 288**

| タブ | グループ | タブ | グループ |
|---|---|---|---|
| ホーム | 9 | データ | 7 |
| 挿入 | 10 | 校閲 | 9 |
| 描画 | 5 | 表示 | 5 |
| ページ レイアウト | 7 | 自動化 | 3 |
| 数式 | 5 | 開発 | 4 |
| — | — | ヘルプ | 1 |
| — | — | Sheet1 | 2 |

**★数え方（自分で 数え直した：2026-08-29）★**
- 正本の行数 = **297**／重なりを除いた三つ組 = **288** ⇒ **★差の9は 全部 まるごと重複★**
  （`データ / データの種類` 4件・`Sheet1 / ヘルプ` 5件）
- **★「その他のオプション」は 0件★**（経営者の説明では「重複と その他のオプションを除いた」と
  なっていたが、この平ら版に その他のオプションは 1件も 無い。**★288という数は 合っている★**）
- **★UIA は「タブ名と同じ入れ物グループ」を1つ返す＝中身が全部 重複する★**（上の9件がそれ）

### ★これを Exally の器にする（司さんの指示・前から出ていた）★

> 「リボンは **★配置なども真似しろ★**」

⇒ **この12タブ・67グループ・288部品の並びを そのまま 器にする**。
⇒ **出来ていない物は ボタンを出さない。ただし ★並びの穴として 一覧には 残す★**（＝数えられる形にする）。

### タブごとの中身（うちの状況つき）

### ファイル
新規／開く／情報／上書き保存／名前を付けて保存／印刷／共有／エクスポート／発行／閉じる／オプション
→ **うち: 読み込む・書き出す・★印刷（2026-08-21 作った）★**。

### ホーム
| グループ | 主な物 | うち |
|---|---|---|
| クリップボード | 貼り付け（形式を選択）・切り取り・コピー・書式のコピー/貼り付け | 一部（形式を選択・書式のコピー **無し**） |
| フォント | フォント・サイズ・太字/斜体/下線・罫線・塗りつぶし・文字色・ふりがな | 大半 在る（フォント種類・ふりがな **無し**） |
| 配置 | 左中右・上下・折り返し・結合と中央揃え・インデント・方向（縦書き/斜め） | 大半 在る（**上下揃え・方向 無し**） |
| 数値 | 表示形式・通貨・パーセント・桁区切り・小数点増減 | 在る |
| スタイル | **条件付き書式**・テーブルとして書式設定・セルのスタイル | ★条件付き書式 在る（2026-08-22・5種類）★／テーブルとして書式設定・セルのスタイル **無し** |
| セル | 挿入・削除・書式（行の高さ/列の幅/シートの整理/保護） | 一部（行列の挿入削除は在る・**高さ/幅の数値指定 無し**） |
| 編集 | **オートSUM**・フィル（下/右/連続データ）・クリア・**並べ替えとフィルター**・**検索と選択** | **オートSUM 在る**（Alt+=）／**フィル 在る**（Ctrl+D・R）／**★並べ替えとフィルター 在る（08-21）★**／連続データ・検索と選択 **無し** |

### 挿入
ピボットテーブル／テーブル／図・図形・アイコン／**グラフ**／スパークライン／スライサー／
ハイパーリンク／テキストボックス／ヘッダーとフッター／記号と特殊文字
→ **うち: 全部 無し**（読み込んだファイルの中のグラフは**壊さず保つ**が、作れない）

### 描画
ペン／消しゴム／数式に変換
→ **全部 無し**

### ページレイアウト
テーマ／余白・印刷の向き・サイズ・印刷範囲・改ページ／背景／タイトル行／拡大縮小印刷／枠線・見出しの表示印刷
→ **全部 無し**

### 数式
関数ライブラリ（財務/論理/文字列/日付/検索/数学/その他）／**名前の定義**／
参照元・参照先のトレース／**ウォッチウィンドウ**／**計算方法の設定**
→ **うち: 関数の挿入(fx) と 数式の入力は在る。名前の定義・トレース・計算方法 **無し**

### データ
外部データの取得／クエリ／**並べ替え**／**フィルター**／区切り位置／**重複の削除**／
**データの入力規則**／統合／What-If分析／グループ化／小計
→ **うち: ★並べ替え（昇順・降順）・フィルター（選んだ値で絞る／解除）・データの入力規則（一覧から選ぶ・整数の範囲）在る（2026-08-21）★。ほかは 無し**

### 校閲
スペルチェック／**コメント**／シートの保護／ブックの保護／変更履歴
→ **全部 無し**

### 表示
標準/改ページプレビュー／**ウィンドウ枠の固定**／分割／並べて比較／ズーム／数式バー・枠線・見出しの表示
→ **うち: ズームは在る（Ctrl+ホイール）／数式バーは在る／★ウィンドウ枠の固定 在る（2026-08-21）★。分割・並べて比較 無し**

### ステータスバー
**合計・平均・データの個数・数値の個数・最小値・最大値**（選択範囲の集計）
→ **★全部 無し★**（選んでも何も出ない）

---

## 5. まとめ（今の数）

| 何 | Excel | うち | 差 |
|---|---|---|---|
| ショートカット（数えた42個） | 42 | **40** | **2**（Esc / F4は編集中のみ） |
| 右クリック（セル） | 51 | 11 | 40 |
| **リボンのタブ** | **12** | — | タブという形が まだ 無い（★作る＝司さんの指示★） |
| **リボンのグループ** | **67** | — | 同上 |
| **リボンの部品** | **288** | **★105★**（対象外24を除いた 相手 264個中） | **159** |
| **画面に在る操作** | — | **332**（同じ物を除くと327） | — |

**★リボンを 配置ごと 作った（2026-08-29）★**

司さん「リボンは前から言うてるけど ★配置なども真似しろ★」
司さん「★訴えられんような見せ方で 同じように★」

| 何 | 数 |
|---|---|
| Excelの部品（正本） | **288** |
| ★対象外（理由つき）★ | **24**（Excelのアドインの仕組み／Claudeアドイン／Microsoftのアプリの宣伝／UIAの写り込み8／★開発タブのVBA記録・危険設定＝うちはVBAを動かさない★／★描画タブの状態表示★／★Office スクリプト＝Microsoftのクラウド（うちはレシピで代える）★） |
| **★うちが 作る相手★** | **264** |
| **★今 押せる★** | **★105（39.8%）★** |

- 並び（タブ12→グループ67→部品288）は **正本 `docs/excel-ribbon-flat.tsv` そのまま**
- **★絵は 1つも 写していない★**＝印は 字（B / I / U / 貼 / ✂ / ¥ / % / Σ …）・色は うちの緑
- **出来ていない物は ボタンを 出さない**。箱は 出して「これから」と 書く（★偽のボタンを 見せない★）
- **★105個 全部 実際に 押して 画面の関数に 届く事を 見張っている★**
  （最初 引数の数を 間違えて **押しても 何も起きないボタン**を 作る所だった。
   ⇒ 動作の層 `lib/ribbon-actions.js` を 1枚 挟み、試験を「在るか」→「押して 届くか」に 変えた）

**★リンク（挿入→リンク）2026-08-30★**

| 何 | 実Excel 16.0 で 測った値 | うち |
|---|---|---|
| 行き先 / 画面の字 / 説明 | Address / TextToDisplay / ScreenTip | 同じ3つ |
| 字の色 | ★BGR 867846 ＝ RGB #467886★ | 同じ（実ブラウザの画に ★#467886の混ざりだけ・黒い点0★） |
| 下線 | ★あり（xlUnderlineStyleSingle）★ | 同じ（実ブラウザで ★横1本 60点★ を 数えた） |
| 押した時 | 1回 押すと 飛ぶ | 同じ。★Ctrl（⌘）を押しながらは 飛ばない（選ぶだけ）★ |

- ★危ない行き先は 入れさせない★＝`javascript:` `data:`（大文字でも 前に空白が在っても 弾く）。
  リンクは ★他人の書類から 入ってくる★＝押した人の画面で 何かを走らせる道に なるため。
- 開く時も ★別の窓＋noopener★（元の画面を 乗っ取らせない）。
- ★未測定★：実Excel の「押したままにすると 選ぶだけになる」は COM から 測れなかった
  （うちは Ctrl を その代わりに した）。

**★見つけて 直した（2026-08-30）★ Ctrl+A で 画面が 死んでいた**

リボンを増やした後の 実UI掃き出しで 出た:
`RangeError: Invalid array length … updateStatusBar → updateBar → sel → onKD`
＝Ctrl+A は `sel(0,0,ROWS-1,COLS-1)`＝★1,048,576 × 16,384 ＝ 17,179,869,184 マス★を
1つずつ 配列に積んでいた（配列の長さの上限 2^32-1 を 超える）。
★死ぬのは 帯だけでは なく、sel() の中で 投げるので「すべて選ぶ」そのものが 何も起きなかった★。
⇒ ★中身が 入っているマスだけ 拾う★（GridStats は 空マスを 数えない＝答えは 1つも 変わらない）。
　列見出し・行見出しのクリック（1,048,576マス）も 同じ道を 通っていたので そこも 速くなった。
　見張り `tests/select-all-statusbar.test.mjs`（21本。★直す前の書き方は ちゃんと死ぬ★事まで 中で 確かめる）

**★このために 新しく 作った 働き★**

| 何 | 真値の 取り方 |
|---|---|
| 上下揃え（上/中央/下） | 実Excelで 実測（**既定は 中央**＝xlsx の中が `<alignment vertical="center"/>`） |
| クリア 3通り（中身だけ/書式だけ/すべて） | 実Excelで 実測（ClearContents は 書式が 残る・ClearFormats は 値が 残る） |
| 数式の表示 | Excel の Ctrl+バッククォート と 同じ働き |
| 再計算（すべて／このシート） | Excel F9 / Shift+F9 |
| ズーム 100% | 上限4倍・下限0.25倍で 止める |
| 重複の削除 | **消した数を 必ず 出す**（黙って 減らさない） |
| フィル（上から／左から） | 前から在る Ctrl+D / Ctrl+R を リボンへ |
| オートSUM | 前から在る Alt+= を リボンへ |
| ★名前の定義（名前付き範囲）★ | 実Excelで 実測（★日本語の名前が 使える★／A1の形と 数で始まる名前は 断られる）。★HyperFormula は 日本語の名前を 受け付けない★ので ★式に入る前に 開く★ |
| ★区切り位置★ | 実Excelの TextToColumns で 実測（★元の列を 上書き★／足りない所は 空） |
| ★グラフ（縦棒・折れ線・円）★ | 実Excelで 実測（★既定は 集合縦棒／360×216／凡例は 系列2本以上／タイトルは 出す★）。★絵も 配色も 写さず 自前の canvas★ |
| ★コメント★ | 実Excelで 実測（★セルに 付く／作者が 付く／既定では 見えない＝右上に 赤い印だけ★）。次／前へ 移る・一覧も |
| ★ページ設定（向き・枠線・見出し）★ | 実Excelで 実測（★縦／A4／余白 上下19.1mm・左右17.8mm／枠線も 見出しも 刷らない★） |
| ★シートの保護★ | 実Excelで 実測（★新しいセルは 全部ロック／ロックは 保護して初めて効く★）。★合言葉は 掛けない＝忘れたら二度と開けない・Excelの合言葉も守りとしては弱い★ |
| ★画面の枠線の 表示★ | ★紙に 刷る枠線とは 別物★（Excelも 別々に 持っている） |
| ★小数の 増やす／減らす★ | Excelの ボタンは ★2つ★（前は 1つで 回る形だった）。★上限30桁は 実Excelで 実測★（勝手に 6桁で 止めていたのを 直した） |
| ★書式のコピー／貼り付け★ | 実Excelと 同じ＝★中身は 写さない。書式だけ★ |
| ★セルの挿入／削除（1つだけ ずらす）★ | 実Excelで 実測（★選んだ セルの 列だけ 動く＝隣の列は 動かない★）。4方向とも 実測と 1マスも 違わない |
| ★行の高さ／列の幅★ | 実Excelで 実測（★既定 17.7pt／54pt・フォントは 游ゴシック 11★） |
| ★セルのスタイル★ | 実Excelから ★中身ごと 読んだ★（標準スタイルは 47個・よく使う9つを 作った）。★COMの色は BGR★なので RGB に 直した（8つとも 一致を 実測） |
| ★並べ替え（2つの鍵）★ | 実Excelの Sort.SortFields で 実測（★同じ値は 2つ目の鍵で 決まる／見出しの行は 動かない／空白は いつも最後★） |
| ★表示の 切り替え（見出し・数式バー）★ | 実Excelで 実測（★既定は 枠線True・見出しTrue・数式バーTrue・数式そのままFalse★）。★実ブラウザで 押して 効く事を 確かめた★ |

**★実ブラウザ（Playwright）で 押して 確かめた（2026-08-29）★**

- **リボンが 高さ637px＝画面の 4分の3 を 食っていた**（グループが 幅35pxまで 潰れ、ボタンが 縦に 12個 積まれていた）
  ⇒ グループを **縮めない（flex: 0 0 auto）**＋**2段まで**にして 横へ 流す ⇒ **たたんだ時 46px／開いた時 219px**
- **狭い画面（560px以下）では 最初から たたむ**（タブの帯だけ 出す）
- **古いツールバーは 畳んだ**＝Excelは リボン1つ。同じ物が 2か所に 在ると ごちゃごちゃになる
  中の 作り込んだ物（サイズの入力・色の見本）は **リボンが 引き取る**（作り直さない＝配線が 生きたまま）
  ⇒ **タブを 移っても 消えない**（描き直す前に 逃がす。★実ブラウザで 踏んで 直した★）
- **クイック アクセス（元に戻す／やり直す）**を リボンの上に（Excelも 同じ場所）。**288の 数には 入れない**
- **実ブラウザで 押して 効いた**：太字／右揃え／パーセント／上揃え
- **PC（1280×800）でも 測った**：リボン 221px → **173px**（タブ45＋中身127）
  ⇒ ★クイックアクセスを タブの帯に 入れた★（別の行だと それだけで 36px 使う。Excelも タイトルの帯に 置いている）

**★未測定を そのままにしない（司さん 2026-08-29「サボるなや」）★**
| まだ 数えていない物 | どう数えるか | いつ |
|---|---|---|
| グラフ／ピボット／図形／ヘッダーフッター | 同じ `tools/ribbon-dump.ps1`（「挿入」タブの中に 居る） | 次 |
| うちの上限（メニューを開いた先） | 実ブラウザ（Playwright）で 全メニューを開いて 数える | 次 |
| VBA | `lib/vba.js` で 実物150本を 読んだ実測が 別に 在る | 済（別表） |

**★「無い」で大きい物（作る材料）★**
1. ~~並べ替え／フィルター~~ → **★どちらも作った（2026-08-21）★**（並べ替え＝昇順・降順／絞り込み＝選んだ値で絞る・解除）。★色やアイコンでの絞り込み・見出しの▼ボタン・2列同時 は まだ★
2. ~~検索と置換（Ctrl+F / Ctrl+H）~~ → **★作った（2026-08-21）★**（★書式で探す・ブック全体・すべて検索の一覧 は まだ★）
3. ~~条件付き書式~~ → **★作った（2026-08-22）★**（セルの値／文字を含む／重複・一意／上位下位／式の5種類・塗り/文字色/太字・押す前に「今 何個 当たるか」を出す・上下で強さを入れ替え）。★データバー・カラースケール・アイコンセット・日付/空白/エラーのルール・xlsxへの書き出し は まだ★
4. ~~ウィンドウ枠の固定~~ → **★作った（2026-08-21）★**（選んだセルの 上と左を固定／A1 は 画面の大きさで変わるので 固定せず理由を出す）
5. ~~印刷／PDF~~ → **★印刷を作った（2026-08-21）★**（PDFは ブラウザの印刷から「PDFに保存」）。★ヘッダー/フッター・1ページに収める・横向き は まだ★
6. ~~データの入力規則~~ → **★作った（2026-08-21）★**（一覧から選ぶ／整数の範囲。★止めるのは「打った時」だけ＝実Excelと同じ★）。★小数・日付・文字の長さ・他のセル範囲を元にする・入力時メッセージ・xlsxへの書き出し は まだ★
7. ~~ステータスバーの合計・平均・個数~~ → **★作った（2026-08-21）★**（平均／データの個数／数値の個数／合計。★数の形の文字やエラーが在れば 黙らずに理由を出す★）。★最小値・最大値を帯に出す・出す項目を選ぶ は まだ★

> ★押して分かった事（2026-08-21 指示役が実配信を押した）★
> 　右クリックのメニューが 高さ743px・画面619px で ★上へ470px はみ出し★ていたため、
> 　★並べ替え・絞り込み・固定・印刷・入力の決まりの5つに 客の手が届いていなかった★。
> 　★置いた≠届く★。直し方は `lib/menu-place.js` と `tests/fixtures/ctx-menu-golden.json`。

8. **グラフを作る**（読み込んだ物は保てる）
9. ~~オートSUM（Alt+=）~~ → **★作った（2026-08-21）★**／連続データのフィルは まだ
10. ~~ショートカット 21個~~ → **★17個 作った（08-18に16個＋08-21にAlt+=）。残り5個★**

★どれから作るかは 司さんが決める★（私は数えて出すだけ）。

---

## 6. この表の使い方

- **1機能 作るたびに この表を更新して、報告に「全部 N／作った M／残り」を書く**
- **★数え直しは機械でやる★**（`tests/excel-parity.test.mjs` が数字のズレを赤にする）
- リボンの一覧に抜けが見つかったら **足してから** 作る（抜けたまま「全部やった」と言わない）

---

## 7. ★答えの差＝司さんの実物 19,323本を 1セルずつ 突き合わせた（2026-08-29）★

測り方 = `node scripts/engine-diff.mjs "<実物.xlsb>"` ／ **★読むだけ・1バイトも 書いていない★**
通した道 = **本番と同じ**（`lib/table-refs.js` → `convertFormula` → シート名を引用符で囲む → **Exally関数プラグイン** → HyperFormula 3.4.0）
**★この測り台が 本番の道と 言える根拠★** = 独自層(`_jsComputeFormula`)が答える式が **このブックでは 0本**（機械で数えた）

| 何 | 本数 | 割合 |
|---|---|---|
| 式ぜんぶ | **19,323** | — |
| Excelが残した答えと **合った** | **★19,323★** | **★100%★** |
| 合わなかった | **0** | 0% |

**★これは 1つのブックの数字★**。他のブックで 100% とは 言わない。

### ★100% までに 直した 4つ★（全部 **実Excel 16.0 で 真値を 測ってから** 直した）

| # | 何が 違っていたか | 本数 | 直した所 |
|---|---|---|---|
| ① | **空のセルを 指した式**（Excel=0／うち=空） | **2,918** | `exally-formula.js` `_hfGetDisplay`（式のセルだけ 0） |
| ② | **.xlsb の 配列の式（記録426）を 拾っていなかった** | **36**（＋道連れ16） | `lib/table-refs.js` `rgceRange` に 426 |
| ③ | **`_xlws.` など Excel内部の印**（.xlsb は 裸で 出す） | 上と同じ式 | `convertFormula` / `lib/xlsx-io.js` `stripXlfn` |
| ④ | **★FILTER が 形を 保っていなかった★** | **10** | `exally-formula.js` `exFilter` |

**★④が 一番 重い★**＝`FILTER` は 平らにして 縦1列で 返していた。
2列以上だと **中身も 形も 壊れる**（実測 `=FILTER(A1:B4,{1;0;0;1})` が `[[100],[2]]`・`COLUMNS=1`）。
司さんの月別合計（**177,523 / 211,558 / 279,558 / 668,639**）が **0** に なっていた。

**★実Excel 16.0 で 測った FILTER の 決まり★**（`tests/filter-shape.test.mjs` に 真値ごと 入れた）

| 式（`A1:B4` = 100,1 / 200,2 / 300,3 / 400,4） | 実Excel |
|---|---|
| `=SUM(FILTER(A1:B4,{1;0;0;1}))` | **505**（縦の条件＝**行**を選ぶ・2列とも 残る） |
| `=COLUMNS(FILTER(A1:B4,{1;0;0;1}))` | **2** |
| `=SUM(FILTER(A1:B4,{1,0}))` | **1000**（横の条件＝**列**を選ぶ） |
| `=SUM(FILTER(A1:A4,{2;0;0;0}))` | **100**（**0以外の数は 真**） |
| `=SUM(FILTER(A1:A4,{-1;0;0;0}))` | **100**（**負も 真**） |
| `=FILTER(A1:A4,{0;0;0;0},"なし")` | **なし** |

### ★SORT / UNIQUE も 同じ壊れ方だった（測って 直した・同じ日）★

「見込み」で 止めずに **測った**。**10項目中 6件が 実Excelと 違っていた**。

| 式（`A1:B4` = 300,1 / 100,2 / 200,3 / 100,2） | 実Excel | 直す前の うち |
|---|---|---|
| `=COLUMNS(SORT(A1:B4))` | **2** | 1 |
| `=ROWS(SORT(A1:B4))` | **4** | 8 |
| `=INDEX(SORT(A1:B4),1,1)` | **100** | 1 |
| `=INDEX(SORT(A1:B4),1,2)` | **2** | `#NUM!` |
| `=INDEX(SORT(A1:B4,2),1,1)` | **300** | — |
| `=INDEX(SORT(A1:B4,2,-1),1,1)` | **200** | — |
| `=INDEX(SORT(A1:B4,1,1,TRUE),1,1)` | **1**（列ごと） | — |
| `=ROWS(UNIQUE(A1:B4))` | **3** | 6 |
| `=COLUMNS(UNIQUE(A1:B4))` | **2** | 1 |
| `=COLUMNS(UNIQUE(A1:B4,TRUE))` | **2** | — |
| `=ROWS(UNIQUE(A1:B4,FALSE,TRUE))` | **2**（1度だけ） | — |

⇒ **行ごとに 並べ替える／行の重複を 消す** 形に 直した。**16項目 全部 実Excelと 一致**。

**★1列の時は 変えていない★**（`=SUM(SORT(A1:A4))` などは 今までどおり）。
実Excel は **空を 残して 後ろに 置く**（`=ROWS(SORT(D1:D4))` → **4**／うちは 落として 2）が、
ここは **`tests/xlsx-harness` の 見本値が 決めている所**なので
**★測っただけで 今回は 変えていない★**（変えるなら 見本値ごと 取り直す）。**これは 残っている 差**。

### ①の 裁定 — ★実Excel 16.0 で 確かめた★（新しい空ブック。司さんの実物は 開いていない）

| 式（C2 は 空） | 実Excelの答え | 画面の字 | うち |
|---|---|---|---|
| `=C2` | **0**（数） | `0` | 空 ← **★違う★** |
| `=INDEX(C1:C5,2)` | **0**（数） | `0` | 空 ← **★違う★** |
| `=IF(C2="","",1)` | `""`（字） | 空 | 空 ← 合う |
| `=C2&""` | `""`（字） | 空 | 空 ← 合う |

**★Excelが 正しい★。決まりは「数の場所なら 0・字の場所なら 空」**。
うちが外すのは 前者だけ（＝④が 0本 なのと 辻褄が合う）。
直す所は **1か所**＝`_hfGetDisplay` が `null` を `''` にしている行。**ただし 式のセルだけ**（ただの空セルまで 0 にしない）。

### ②の 根っこ — ★記録426（配列の式）を 拾っていない★

- 読み取りライブラリの `case "PtgList": f.push("Table"+idx+"[#"+rt+"]")` が **列名を捨てる**のは **★既知★**（`lib/table-refs.js:1-33` に 書いてある）。**.xlsb だけ**（.xlsx は `<f>` の字をそのまま使うので 起きない）。
- `table-refs.js` は それを 直す部品で、今回も **15,734本 直した／断り 0**。
- **★残る52本は「断った」のではなく「見ていない」★**。拾う記録が **id 8/9/10/11 の4種だけ**で、
  `月別`(sheet7.bin) に **★id 426（配列の式）が 36本★** ある。実測:

  | シート部品 | 式系の記録 | PtgList を持つ記録 |
  |---|---|---|
  | sheet7.bin（月別） | `9:130` **`426:36`** | `9:166` |

- **36本（FILTER の式）＋ そこを合計している 16本 ＝ 52本**。関数ごとの内訳（FILTER 36／DAY 36／ROUNDDOWN 36）とも 合う。
- 画面には **`#ERROR!`** が出る。実Excel は **177,523 / 211,558 / 279,558 / 668,639** など **★金額★**。
- 直すには `rgceRange` に **426 の並び**を教える必要がある（今は null を返す）。**★別の仕事★**。
